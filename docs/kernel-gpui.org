#+TITLE: closure — kernel + gpui work queue
#+STARTUP: overview

Scope is the kernel (closure-org, closure-core, closure-store,
closure-query, closure-config, closure-input, closure-undo,
closure-eval, closure-whichkey, closure-shell-core), the gpui shell,
and closure-cli only where the window needs it. Every other crate is
frozen: it keeps compiling and stays green, and gains no features.

Rules for this file:

- One =* TODO= per deliverable, small enough to finish in one commit.
- Every item carries an =Acceptance:= line that is a runnable command.
- Items may be ADDED at any time. Every bug found and every "we
  should" gets written down here before it is acted on.
- An item is never deleted, reworded, or merged to shrink the queue.
  The count drops only by finishing work.
- TODO -> DONE only after the commit lands, with the answer in the
  item's body.

* DONE Spec: define what a block is
The first item in the vision is composability and it is the least
built. Nothing under "Composition" below can be tested until this
says what composes with what.

Decide and write into =docs/spec.md=: is the unit a headline, an
element, or both? What may appear inside a composed block? What is
its identity under I2? What does composition mean for I1 roundtrip —
does an expansion ever reach the file, or only the view?

Acceptance: =grep -c "^### I12" docs/spec.md= prints 1, and
=nix develop -c cargo test -p closure-shell-core --test what_a_block_is= passes.

Three tiers: an *addressable block* is a headline and is the only
thing with an identity (I2); a *content block* is an org element and
is what a template is made of; a *composable block* is a named widget
with declared inputs. A widget never contains a headline — a template
that emitted one would either mint the same id at every call site or a
new one on every render, breaking I2 or I6.

The load-bearing half is I12: an expansion is never written back.
Org's dynamic blocks work the other way round and closure cannot,
because then a file's bytes would depend on when it was last
refreshed. That keeps composition inside `query`, one of the seven
kernel primitives, so it adds no eighth.

(The acceptance line named a `spec_is_true` test that does not exist;
the check it meant is the one above, which does.)

* DONE Spec: I11 performance budgets, with numbers
"Almost no input lag" cannot be tested. State p99 keystroke->frame,
cold vault open, and a memory ceiling, each at a stated headline
count, and say which are machine-pinned and which are portable
shapes.

Acceptance: =grep -c "^### I11" docs/spec.md= prints 1.

Stated per *headline*, not per vault — which is what makes them
portable and therefore worth failing a build over. 100 us/headline to
open, 8 KB/headline resident, 1000 us to derive one keystroke;
measured at 2.9 us, 2 KB and 3 us. Order-of-magnitude guards: they
fail on an accidental quadratic, not on a slow laptop, and the shape
tests remain the sensitive ones.

No frame budget, deliberately: this machine has no Vulkan driver, so a
frame time measured here would describe lavapipe rather than closure.
The question the kernel owns is what a keystroke makes it derive.

* DONE Spec: state the target scale
The store parses the whole vault into memory. That is a load-bearing
decision made by accident. Say the number the kernel promises to
hold.

Acceptance: =grep -ci "target scale" docs/spec.md= is at least 1.

100,000 headlines — about 15 MB of org. Measured on this machine,
release: 10k headlines open in 32 ms for 27 MB resident, 100k in
285 ms for 196 MB, both linear at ~2.9 us and ~2 KB each. A million
would be ~3 s and ~2 GB, which is where parsing the whole vault stops
being the right design, so the promise stops below it on purpose.
=closure_store::TARGET_SCALE_HEADLINES= carries the same number and
=closure-store/tests/target_scale.rs= fails if the two disagree.

* DONE Widget: named parameters at the call site
=({{name}})= substitution is =#define=, not composition. A call site
must be able to pass values.

Acceptance: =nix develop -c cargo test -p closure-query --test widget_params=

A widget declares what it takes (=:inputs who,title=) and a reference
carries arguments (={{card title=Today note="write the parser"}}=).
Inside the body a parameter is read with the same ={{who}}= a widget
reference uses, and an argument shadows a widget of the same name —
locals beat globals, so a widget can be written without knowing every
name in the vault.

The declaration is not ceremony: a widget block is its own call site
with no arguments, so ={{who}}= in an undeclared widget cannot be told
apart from a reference to a widget nobody defined, and that has to
stay an error. Declared and unbound expands to nothing; undeclared is
still a widget lookup, so a typo is still an error.

Verified in the window only as far as the window allows — it shows the
template verbatim and never an expansion, which is I12 doing its job
for the editor and a missing read view everywhere else. Written down
as its own item.

* DONE Widget: typed inputs validated at load
I9 says config errors fail the load. A widget's declared inputs are
config. A missing or wrong-typed argument is an error before render,
not a hole in the output.

Acceptance: =nix develop -c cargo test -p closure-query --test widget_types=

=:inputs count:number,title,done:bool=. Three types, because a type
system here earns its place catching the mistakes that are always
mistakes and stops earning it the moment a template needs a grammar to
describe its own arguments. Untyped stays text, which is what every
input written before today is.

Two errors, both carrying everything needed to fix them without
opening another file: =UnknownArgument= for a call site naming an
input that does not exist (the typo that used to render as silence),
and =BadArgument= for a value the type cannot hold. Missing is
deliberately not an error — a definition block is a call site with no
arguments, and showing a template is not a mistake.

Not visible in the window yet: nothing paints widget errors, or
widgets. Both are items of their own.

* DONE Widget: slots
Content passed *into* a widget body, so a widget can wrap what it is
given rather than only emit what it knows.

Acceptance: =nix develop -c cargo test -p closure-query --test widget_slots=

={{...}}= has nowhere to put a block, so the call site that carries
content is a block itself: =#+BEGIN: closure-widget :call panel :with
title=Notes=, body is the content, ={{slot}}= inside the widget is
where it lands. =:name= defines, =:call= invokes — one keyword apart,
and both are ordinary org dynamic blocks, so a file full of them still
opens in Emacs.

=slot= is always in scope, empty when there is none, so ={{slot}}= in
a widget nobody passed content to is not mistaken for a widget nobody
defined. The content is expanded in the caller's scope before the
callee sees it, the same rule as an argument's value.

Verified on :1: a panel wrapping two list items.

* DONE Widget: cycle detection with a depth limit and a named path
An expansion cycle must fail with the chain that caused it, not a
stack overflow and not a silent stop.

Acceptance: =nix develop -c cargo test -p closure-query --test widget_cycles=

The error carries the whole ring, from where it closes rather than
from where expansion began — the widgets before it are on the way in,
not part of it. On :1: =widget cycle: page -> panel -> header -> page=
in the pane where the widget is.

Depth is a separate failure and now has its own error. A chain nesting
5,000 deep without ever repeating a name is not a cycle, and recursing
it would end the process — which I5 forbids more strongly than it
forbids a panic, because a blown stack takes the window with it. The
limit is 64: far past what a page needs, far below what this expander
can survive.

* DONE LSP: composition errors as diagnostics with precise ranges
Acceptance: =nix develop -c cargo test -p closure-lsp --test diagnostics=

They were already diagnostics; they were all reported at the first
=#+begin: closure-widget= line in the file, whatever had gone wrong
and wherever it happened, so an editor underlined a block header
several lines above a reference that was perfectly visible. The error
already knows which name, which argument or which value is at fault,
so the range points at it — and never at the line that *defines* the
widget, which is where a name is declared rather than used wrongly.

A depth failure still lands on the block header: there is no single
reference to blame for a nest that went too deep.

* DONE gpui: render a composed widget where it is written
Found 2026-08-11 while adding parameters. =widget_node= has no callers
and nothing in either shell crate builds a =Node::Widget=: composition
exists in closure-query and in the CLI, and the window shows the
template verbatim and never the expansion. Showing the source is
right for the editor (I12 — the file holds the template), so this is
about the read view.

Until this exists there is no way to see composition in the window at
all, which blocks the visual half of every item under Composition.

Acceptance: =nix develop -c cargo test -p closure-shell-core --test widget_preview=

The detail pane expands; the editor does not. That split is I12 rather
than a preference: the editor writes back what it shows, so it shows
the file, and what it cannot show it must not be able to destroy. The
preview reads and never writes, so it shows what the template means.

The defs are memoised per revision — collecting them walks every
document, and landing on a headline must not (I11) — and the memo
holds =Option<u64>= rather than a bare revision, because a fresh vault
is at revision zero and so is a zeroed memo: the two agreed, the empty
map looked like a vault defining no widgets, and every composition in
it failed as unknown.

Verified on :1: two ={{card ...}}= calls render as table rows with
their arguments substituted and their typed values intact.

* DONE Preview: decide whether a composition shows its delimiters
The pane shows the =#+BEGIN:= / =#+END:= lines around an expansion.
Arguable either way — they say "this is composed rather than typed" —
but nobody decided it, and a read view showing syntax is a choice that
should be made on purpose.

Acceptance: written into docs/spec.md beside I12 and held by =widget_preview.rs=.

They stay. Without them the pane shows content that appears nowhere in
the file, and a reader who opens the editor to change what they are
looking at finds a template instead. The delimiters are the seam: they
say "this is composed rather than typed", which is what a reader needs
to know before reaching for =i=.

* DONE gpui: show a widget's expansion error where the widget is
A widget that fails to expand currently contributes nothing. The pane
must say what failed and why, in place.

Acceptance: =nix develop -c cargo test -p closure-shell-gpui --features gpui-test --test widget_errors=

The reason travels beside the text rather than inside it
(=Detail::composition_error=) and is painted in the error colour under
the preview. A failure is the one thing a pane shows that is about the
document rather than in it, and a shell that cannot tell those apart
paints an error as prose. The template stays visible underneath, so
there is something to fix.

On :1: =widget cycle: page -> panel -> header -> page= in red, with
the template above it.

* DONE Org conformance: harness and pass-rate command
A corpus of real org files and a command that reports what fraction
roundtrip byte-exact. Publishing the number is what makes "org
compatible subset" mean something.

Acceptance: =nix develop -c ./target/debug/closure conformance= prints a percentage.

The measure in the item is the wrong one, and checking took two
minutes: a file carrying =#+TBLFM:=, a repeater, a LaTeX fragment and
an entity roundtrips byte-exact today, because the parser is
span-preserving and anything it does not recognise comes back
verbatim. A roundtrip rate over real org would read near 100% while
none of those four did anything.

So conformance is a matrix of 34 constructs and the rate is the
fraction *understood* — semantics closure can act on, with a test
behind the claim. 61% (21 of 34) as of today. Every "understood" row
names a test file, and the ratchet fails if one of them stops
existing, because a matrix nobody checks is a marketing document.

* DONE Org conformance: enforce the rate as a ratchet
Acceptance: =nix develop -c cargo test -p closure-org --test conformance_ratchet=

Floor at 61%, raised by each of the seven org items below. Also holds
that no construct is listed twice, that the matrix is at least 30 rows
(small enough is the other way to get a flattering number), and that
something in it is still only preserved — a matrix where everything is
understood is one that has stopped looking at what org has.

* DONE Org: timestamp repeaters
=+1w=, =.+1d=, =++1m= on SCHEDULED/DEADLINE. A task tool without
repeaters is not a task tool.

Acceptance: =nix develop -c cargo test -p closure-org --test repeaters=

All three kinds, all five units, parsed and advanced. They differ only
in what "next" means when you are late, which is the whole reason org
has three: =+1w= lands on the next date in the original series, =++1w=
skips what was missed, =.+1w= counts from today. A warning period
(=-2d=) is not a repeater and is not read as one.

Conformance 61% -> 67%.

Civil-date arithmetic moved from closure-shell-core down to
closure-org on the way: it is arithmetic on org timestamps, and the
layer that parses them is where it belongs. shell-core re-exports the
names it was written against.

* DONE Kernel: marking a repeating task DONE advances its date
Parsing a repeater is not the feature; the feature is that finishing
the weekly review schedules the next one instead of closing it. That
is a command, and I3 says every mutation is undoable, so it belongs in
closure-core rather than in a shell.

Acceptance: =nix develop -c cargo test -p closure-core --test repeat_on_done=

Three changes that undo as one: the keyword goes back to TODO, the
date moves on, and =:LAST_REPEAT:= records when this occurrence was
finished. One undo getting the keyword back but keeping next week's
date would leave a document that never existed, so it is a single
=Edit::Repeat= rather than three edits.

Needed =rewrite_headline_remove_property=, which did not exist: undo
has to leave the drawer as it found it, and setting a property to an
empty string leaves =:LAST_REPEAT: = behind. Removing the last entry
takes the drawer with it, for the same reason.

End to end: =closure set-todo tasks.org <id> DONE= on a =+1w= task
moved 2026-08-11 to 2026-08-18 and left it TODO.

* DONE Org: #+INCLUDE resolution
Acceptance: =nix develop -c cargo test -p closure-org --test include=

Composition by file, and it gets the composition rules already
settled, which is the point of having settled them: the expansion is a
view and is never written back (I12), a cycle names its ring rather
than recursing until the stack goes, and a depth limit catches a nest
that is deep without being circular (I5).

The directive line is dropped from the view — showing both the
directive and its result tells the reader something the file does not
say. =:lines "2-3"= selects part of a file, one-based and inclusive,
the way org writes them and the way an editor numbers the lines you
are reading them off.

Conformance 73% -> 76%.

* DONE Org: entities and LaTeX fragments
Acceptance: =nix develop -c cargo test -p closure-org --test entities=

Two constructs in one commit because they are the same question asked
twice: which run of characters in this line is maths or a named
character rather than prose. =fragments()= answers it; =entity_char()=
gives the glyph for the names closure knows, and =None= rather than a
guess for the rest.

The interesting rule is the dollar. A digit just after an opening =$=
is money, which is what separates "$5" from "$x^2$" — and a digit just
before the closing one is ordinary, because =$x^2$= ends in a 2. The
first version applied the digit rule to both ends and rejected all
maths; the test for =$x^2$= caught it immediately.

Conformance 67% -> 73%.

* DONE Org: inline tasks
Acceptance: =nix develop -c cargo test -p closure-org --test inline_tasks=

Fifteen stars is org's inline task: a task attached to the paragraph
it sits in rather than a section of the document. closure read it as
an ordinary level-fifteen headline, so a note with one in it had a
branch in the outline that is not one and a headline count that was
wrong.

The parser stops at fifteen and =inline_tasks()= finds them instead —
not outline structure is not the same as invisible, since an agenda
wants them, which is why anybody writes one. Fourteen stars is still
an ordinary headline; the boundary is org's and a parser that guesses
turns deep outlines into asides.

Conformance 76% -> 79%.

* DONE Org: column view (#+COLUMNS)
Acceptance: =nix develop -c cargo test -p closure-org --test column_view=

=%25ITEM(Task) %TODO %EFFORT{+}= — width, property, title, summary.
A file saying how it wants to be tabulated is the same idea a
=closure-view= block expresses from the other end; this is the part
that lets the *file* ask.

Braces are read before parentheses: a summary can follow a title, and
cutting at =(= first would leave the brace inside the title. An
unrecognised summary is kept as no summary rather than refused — org
has a dozen and closure knows four, and a format nobody implemented
should not make a document unreadable, the same rule an unknown widget
input type and an unknown rollup already follow.

Conformance 79% -> 82%.

* DONE Org: #+TBLFM table formulas
Acceptance: =nix develop -c cargo test -p closure-org --test tblfm=

Reads and evaluates; writing the answers back into the file is a
mutation and so is a command, undoable, and its own item below — the
same split composition landed on, for the same reason.

The subset is column assignments over the four operators, numeric
literals and =$N= references, left to right without precedence, which
is what the formulas people actually write look like. Org's ranges,
=vsum=, =@row= addressing and lisp are not here and the matrix says
so by what it does not claim.

A row whose referenced cells are not numbers keeps the target cell it
already had, and so does a division by zero: blanking it would lose
what the author typed, and a formula is not a licence to do that.

Conformance 82% -> 85%.

* DONE Kernel: recomputing a table's formulas is a command
=eval_table_formulas= says what the cells should be; nothing puts them
back. In org that is =C-c C-c= on the formula line, and here it is a
mutation — so it belongs in closure-core behind the registry, with an
Edit the undo tree can reverse (I3/I8).

Acceptance: =nix develop -c cargo test -p closure-core --test recompute_table=

One edit for the whole table rather than one per cell: a
half-recomputed table is a table that never existed, and undo has to
be able to say so. The header and the separator are left alone — a
formula is about data rows, and computing over the header would put a
number where the column's name goes. Rows are rebuilt at the widths
the file already used, so a recompute changes values and not layout.

* DONE Org: #+CALL and noweb references
Acceptance: =nix develop -c cargo test -p closure-eval --test noweb=

={{<<name>>}}= is what lets a literate document explain a program in
the order a reader needs and assemble it in the order the compiler
needs, which is the whole argument for writing one. Tangling wrote the
reference out verbatim, so the assembled file had =<<setup>>= where
the setup should have been.

Resolved when tangling now, and a reference to nothing fails the
tangle rather than writing it: a shell script containing =<<nosuch>>=
fails later, somewhere else, with a worse message.

Two details that needed deciding. Only a reference alone on its line
is one — =$((1 << 2))= is a shift, and a parser that says otherwise
finds references in arithmetic. And the indentation at the reference
is applied to every line that replaces it, because a block being
pasted into Python or YAML is being pasted where indentation is
syntax.

Conformance 85% -> 91%.

* DONE Databases: filters in the view definition
Acceptance: =nix develop -c cargo test -p closure-query --test view_filters=

Already built before this session, and tested — the item existed
because the gap analysis that seeded this queue was wrong. Verified by
running it rather than by reading: =view.rs= covers equality, not-equal, substring, numeric >, and several filters ANDed. No code was needed and
none was written.

* DONE Databases: multi-key sorts
Acceptance: =nix develop -c cargo test -p closure-query --test view_sorts=

Already built before this session, and tested — the item existed
because the gap analysis that seeded this queue was wrong. Verified by
running it rather than by reading: =view.rs= covers =:sort todo,title= and =:sort todo,-title=, plus numeric-not-lexical ordering. No code was needed and
none was written.

* DONE Databases: grouping
Acceptance: =nix develop -c cargo test -p closure-query --test view_groups=

=:group todo= on a view. Rendered as one org table with a rule between
groups rather than as several tables: it is one query with one set of
columns, and a reader scanning down a column should not have to
re-find it after every heading. =|---|= is an ordinary separator, so
the result is still a table Emacs will align.

Grouping by a column that is not displayed works — the value is
extracted for the grouping and dropped again before rendering. A row
whose group value is empty keeps its own group rather than vanishing,
because hiding it would make the table disagree with the query that
produced it. Without =:group= everything is one unnamed group, so the
renderer has one shape to draw rather than two.

* DONE Databases: relations between headlines
The thing that makes a Notion database more than a saved search.

Acceptance: =nix develop -c cargo test -p closure-query --test relations=

=rel:PROJECT= as a column: the property holds another headline's id
and the column shows that headline's title. A ULID is the one string
in a vault a reader can do nothing with.

Spelled as a column rather than a new =:relation= directive, because
=:columns= already answers "what does this name mean" and a second
place to say it is how two places get out of step. It groups, so a
board of tasks by project is =:group rel:PROJECT=.

A relation that points nowhere renders =?<id>= rather than blank — a
dangling id is a mistake in the vault, and the reader has to be able
to tell it from "no project at all". And the link survives a rename,
which is the whole reason to have one instead of writing the name into
a property by hand; there is a test for exactly that.

* DONE Databases: rollups over a relation
Acceptance: =nix develop -c cargo test -p closure-query --test rollups=

=rollup:PROJECT.EFFORT:sum= — the relation to follow back, the
property to read on the rows that come back, and what to do with them.
A relation reads forwards, a rollup reads the same edge backwards and
does arithmetic on it, which is the half of a database people stare
at.

Four aggregates. Sum and count of nothing are zero, because a project
with no tasks has an effort of zero and that is a statement; min and
max of nothing are blank, because the smallest of no numbers is not
zero and saying zero would be a wrong answer rather than a missing
one. A value that is not a number is skipped rather than read as zero
— a total poisoned by one typo is worse than one that is quietly
short. An unrecognised aggregate falls back to count, the same rule an
unknown column already follows.

End to end: a table of projects with =count(EFFORT)= and =sum(EFFORT)=
beside each, and a table of tasks grouped by =rel:PROJECT=.

* DONE Databases: formula columns
Coda-style, in the user's language, through closure-eval — not a
custom expression syntax.

Acceptance: =nix develop -c cargo test -p closure-query --test formula_columns=

Already built before this session, and tested — the item existed
because the gap analysis that seeded this queue was wrong. Verified by
running it rather than by reading: =closure view --formula 'cut -f2 | awk "{print \$1*2}"'= computed a doubled column; covered by =closure-eval/tests/formula.rs=. No code was needed and
none was written.

* DONE gpui: render a database view
Acceptance: =nix develop -c cargo test -p closure-shell-core --test db_saved_views=

The pane built four fixed columns over every headline in the vault and
never looked at what the vault had defined, so filters, sorts,
grouping, relations and rollups — everything =closure-query= grew —
were invisible. A saved view nobody can see is a query nobody runs.

Now the vault's first =#+BEGIN: closure-view= wins, and the everything
table is what a vault with no saved view still gets. The header line
names the view, because a table with no name cannot be told from the
default one. On :1: =database — open-tasks · 3 row(s)= against six
headlines, so the filter really ran.

* DONE The database picker shows the default columns, not the view's
Found 2026-08-11. =M-x db-view= floats a picker over the table, and
the picker's three fields are the *default* table's columns — title,
keyword, tags. A saved view's columns, including relations and
rollups, are not among them, so the view is filtered and sorted
correctly and then displayed as something else.

Written down first as "the surface opens behind its own picker", and
that was wrong: the picker is deliberate and was asked for — "db-view
doesn't render the results in command palette", with three tests
holding it. Removing it was the wrong fix and was reverted. What is
missing is that the picker does not know about saved views.

Acceptance: =nix develop -c cargo test -p closure-shell-core --test db_graph_picker=

The picker's first field is the view's first column and the rest go in
the detail as =NAME: value=, so a relation resolved to a title and a
rollup arrive with everything else. Titled after the view.

On :1: =db · open-tasks=, three rows, each reading
=PROJECT: Rewrite the parser · EFFORT: 3=.

Worth remembering how this nearly went wrong. The first fix removed
the picker, three shipped tests failed, and they turned out to be
holding a behaviour the user had asked for by name. Reverted. Reading
the tests for a surface before changing it would have cost a minute
and saved the detour.

* DONE gpui: edit a database cell through a command
I8 — the pane mutates only through the registry.

Acceptance: =nix develop -c cargo test -p closure-shell-core --test db_edit=

The addressing is the feature, not the writing. After a filter, a sort
and a grouping, row 0 of the table is not row 0 of the vault, so the
row is resolved back to a block through the same spec that produced it
— an extra =Column::Id= on that spec rather than a second ordering
somebody has to keep in step. Writing is then an ordinary command:
=set-property=, or a rename for the title column, undoable like
everything else.

Which columns refuse is the other half. A relation shows a title and
holds an id; a rollup is arithmetic over other rows. Typing into
either writes a value the next render throws away, so both say why
instead.

* DONE Perf: keystroke->frame budget at the stated scale
Machine-pinned, beside the portable shape test that already exists.

Acceptance: =nix develop -c cargo test -p closure-shell-gpui --test budget_keystroke=

9.5 us per keystroke in debug against the 1000 us ceiling, and 211 us
for the single landing on a 5,000-section chapter. Not a frame budget:
this machine has no Vulkan driver, so a frame time measured here would
describe lavapipe. The acceptance line said =--features gpui-test=; it
does not need the feature, since what it measures is the kernel's
derivation rather than anything painted.

The 211 us is a residual and has an item of its own below.

* DONE Perf: cold vault open budget
Acceptance: =nix develop -c cargo test -p closure-store --test budget_open=

Measured 9.9 us per headline in debug at 10,000 against the 100 us
ceiling, plus a shape test — four times the vault must not take more
than four times as long, which is what an accidental quadratic trips
over long before the ceiling does. Proven to bite by running it once
at 1 us before setting the real number.

* DONE Perf: memory ceiling at the stated scale
Acceptance: =nix develop -c cargo test -p closure-store --test budget_memory=

1 KB per headline measured against the 8 KB ceiling, read from
/proc/self/statm — resident set, not the allocator's own accounting,
which does not see what it has handed back to the OS. Where there is
no procfs the test says so on stderr instead of passing quietly.

* DONE Store: hold the target headline count
Whatever the spec item above commits to, the store has to actually
open it and answer queries inside the budgets.

Acceptance: =nix develop -c cargo test -p closure-store --test target_scale_holds=

The other budget tests run at 10,000 because that is fast enough for
every build; this one runs at the promised 100,000, because a
per-headline cost measured at a tenth of the scale is an
extrapolation, and extrapolating is how a promise about 100,000 gets
made by someone who has only opened 10,000. Opens and stays inside
both budgets in 1.3 s in debug, and still resolves an id and a
backlink from the far end of the vault.

* DONE Bug: the window stops repainting after a fast key burst
Found 2026-08-09 while verifying the traversal fix. On a ~2400
headline vault, after ~400 synthetic keypresses the window is
focused and active per xdotool, sits at ~1% CPU, takes no further
input, and does not repaint even on resize. A 3-headline vault
survives the identical burst. Observed on the lavapipe software
rasteriser, which may or may not be the cause.

Acceptance: diagnosed, and what the program tells the user matches.

Every guess in that note was wrong except the symptom.

*Holding* =j= — the X server's own auto-repeat, which is what a person
does — advances the selection one row and then the window never
repaints again. Not 400 synthetic keys: two ordinary ones.

It is not vault size: three headlines wedge like 2,400. Not window
size: 520x320 wedges like maximized. Not input rate beyond the first
frames: 600ms apart works, 33ms apart does not.

And it does not take input away. Forty keystrokes sent to a wedged
window cost the process forty milliseconds of CPU while the screen did
not change by a single pixel — so closure is still listening and still
advancing its state behind a frame that is stuck. The main thread
sleeps in a timer at ~1% CPU; the WSI swapchain threads wait on
futexes. That is the presentation path, not closure's input handling
and not the kernel.

Which means the traversal work is invisible on this machine and
correct anyway: the freeze the report was about is real on a machine
with a GPU, and this is a different failure underneath it.

What closure can do about it, and now does: say so. "Frames are slow"
was the old warning, and it is wrong in the way that matters — a
reader watches the window stop and has no reason to connect the two.
It now says the window may stop repainting, that closure is still
reading keys, and to use =closure tui=.

* DONE Conformance measures the parser, not what closure offers
Found 2026-08-12 by grepping for callers. Seven of the nine org APIs
this run added have none: =inline_tasks=, =document_columns=,
=columns_format=, =fragments=, =entity_char=, =resolve_includes=,
=call_target=. The matrix calls those constructs "understood" because
there are semantics and a test, and that is true — and it is not the
same as a reader being able to see them.

=describe_command= was the same shape: a tested function nothing could
reach. The rule was stated then and applies here.

Each unwired API gets an item below. This one closes when the matrix
distinguishes "the parser understands it" from "a shell offers it", so
the number stops being able to drift away from the experience.

Acceptance: =closure conformance= reports both, and the ratchet holds both.

91% understood, 88% offered. The gap is the entire point: before the
unwired parsers were wired up it was 91 and 70, and nothing about the
first number would have shown that. Both are ratcheted, an =offered=
claim must name a test that exists — the guard caught three citations
I had assumed rather than checked, on its first run — and nothing may
claim to be offered without being understood.

One construct is =parsed only= now: footnote references. Three are
preserved: radio targets, org's own macros, =#+SETUPFILE=.

* DONE Entities and LaTeX render in the read view
=\alpha= shows as a backslash and a word. The rule is already settled
by I12 — the editor shows the file, the preview shows what it means —
and this is the same split for a different construct.

Acceptance: =nix develop -c cargo test -p closure-shell-core --test entity_preview=

On :1: "the α particle decays to β -> γ", "20°", "± 2", "€ 5", "1– 10
and …". LaTeX deliberately untouched — =$x^2 + y^2$= needs a
typesetter and a half-rendered formula is worse than the source, which
at least says what it is.

An entity closure has no character for is left exactly as written, and
that is also what keeps =C:\notes= a path rather than a rendering
accident. There is a test for the path.

* DONE Inline tasks reach the agenda
=inline_tasks()= finds them and nothing asks. An inline task is a task
attached to a paragraph; the agenda is where tasks are read, and one
that is invisible there is one nobody writes twice.

Acceptance: =nix develop -c cargo test -p closure-shell-core --test inline_agenda=

=inline_task_rows= gives =(context, title, path)= — the context being
the headline it hangs under, because "ring the plumber" is only
actionable beside "Kitchen". Finished ones are listed too: which to
show is the reader's decision, and hiding them stops anyone asking
what was done today.

Visible without being structure: the parser takes inline tasks out of
the tree on purpose and there is a test that this does not put them
back.

The first version found context by taking lines starting with =*= but
not =***=, which is right for a two-level file and wrong for every
other one — a task under a level-four headline got the level-one
headline above it. Caught by adding the case rather than by noticing.

* DONE #+INCLUDE resolves in the read view
Same rule as a widget: the file keeps the directive, the preview shows
what it names. =resolve_includes= exists and no shell calls it.

Acceptance: =nix develop -c cargo test -p closure-shell-core --test include_preview=

Third construct through the same seam, so the rule is worth stating as
a rule rather than rediscovering each time: I12 splits every one of
these the same way, and widgets, entities and includes are one
decision applied three times. A file that is not there is reported
where the composition errors are, because a directive resolving to
nothing must not read as an empty note.

* DONE #+COLUMNS drives a database view
A file that declares how it wants to be tabulated should be tabulated
that way. =document_columns= parses it; =ViewSpec= renders columns;
nothing connects them.

Acceptance: =nix develop -c cargo test -p closure-query --test columns_view=

=ViewSpec::from_document_columns=. Org's column view and closure's
database view turned out to be the same idea from opposite ends — one
says "here is a table of the vault", the other "here is how *this
file* should be read" — so the join is a constructor rather than a
feature.

Scoped to the file that declares it, because the directive is a
statement about the document it sits in. =ITEM= maps to the title and
everything else is a property, which is what =:columns= already means.
=None= when a file declares nothing, since a caller wants to know
whether to show a table at all.

* DONE #+CALL runs the block it names
=call_target= reads the line and nothing evaluates it. Half a feature
is the half that looks finished from the matrix.

Acceptance: =nix develop -c cargo test -p closure-eval --test call_runs=

=run_call= goes through the same backend and the same trust check as
any other block — a call that could run what an ordinary block may not
would be a way around the check rather than a feature, and there is a
test that an untrusted language is refused.

The block is assembled before it runs, so ={{<<setup>>}}= inside a
called block means what it means anywhere else. Noweb assembles and
=#+CALL= evaluates; between them that is what a reusable block is.

* DONE Footnotes are readable where they are referenced
The one construct =closure conformance= still calls "parsed only". A
footnote you cannot read is a pointer to nothing: =[fn:1]= in the body
and the text somewhere else in the file, or in another file, with no
way to see them together.

The read view already resolves compositions, entities and includes.
This is the same seam and the same rule.

Acceptance: =nix develop -c cargo test -p closure-shell-core --test footnote_preview=

=footnote_rows= gives =(name, text)= for the notes the selected
headline cites, gathered from wherever in the vault they are defined.
The reference stays where the author put it — a marker is part of the
sentence, and replacing it inline would change what the sentence says
— and the text goes underneath, which is where a footnote goes.

A reference with no definition is listed with a =?= rather than
dropped: a dangling footnote is a mistake in the file, and showing
nothing hides it. The same call the dangling relation made.

Conformance is now 91% understood and 91% offered — nothing is parsed
and invisible.

* DONE Org: macros ({{{name}}})
=#+MACRO: name replacement= defines and ={{{name}}}= uses. Preserved
and never expanded. No collision with closure's widgets: those are two
braces and these are three, and the two ideas are close enough that
sharing an expander is the obvious mistake to avoid.

Acceptance: =nix develop -c cargo test -p closure-org --test macros=

A separate expander from the widgets on purpose, with a test that
={{card}}= survives the macro pass and ={{{project}}}= does not
survive it. Sharing one would have given org's macros closure's rules
or the reverse, and org's meaning is not closure's to change.

Offered in the same pass as entities and includes, and =traversal_cost=
caught the cost of doing it naively within a minute: reading the
document's source per keystroke to find =#+MACRO:= lines put the whole
file back into every landing. Guarded behind a =contains("{{{")=,
which is not an optimisation but the difference between free and
O(file).

Conformance 91% -> 94%, understood and offered both.

* DONE Org: #+SETUPFILE
=#+SETUPFILE: "theme.org"= pulls another file's keywords in — the same
shape as =#+INCLUDE=, and =resolve_includes= already exists, so this
is about scoping to keywords rather than a second reader.

Acceptance: =nix develop -c cargo test -p closure-org --test setupfile=

Not =resolve_includes= with a different name, which was the tempting
shortcut. An include pulls in content, a setupfile pulls in settings —
=#+MACRO:=, =#+TODO:=, =#+COLUMNS:= — and nothing else, which is what
lets a vault keep one file of shared definitions without every
document referencing it inheriting its prose. There is a test that the
prose stays behind.

A missing setupfile is silence rather than an error, because it costs
settings and not the document; a missing include fails because its
content was meant to be read. Chains resolve, rings stop.

Wired so a macro from a setupfile is as usable as a local one, which
is the whole point of having one.

Conformance 94% -> 97%.

* DONE Org: radio targets
=<<<name>>>= makes every later occurrence of "name" a link to it. The
last preserved construct, and the only one whose cost is per *word*
rather than per line — worth measuring against I11 before wiring it
into a shell.

Acceptance: =nix develop -c cargo test -p closure-org --test radio_targets=

One pass over the text per call rather than one per target, with a
test that four times the text costs less than eight times as long: a
scan-per-target is quadratic the moment somebody writes fifty of them,
and this is the one construct whose work is per word. Targets are
memoised per revision in the shell for the same reason the widget
definitions are.

Whole words only — "enclosure" contains "closure" and is a different
word — and a definition never matches itself, or a target links a word
to where it already is.

Finishing it took the matrix to 100%, which fired
=the_matrix_is_worth_having=: a matrix where everything is understood
has stopped looking at what org has. That guard was written to catch
exactly this and it was right. Eight constructs closure genuinely does
not handle went in — statistics cookies, =#+STARTUP:=, clocktables,
export attributes, time ranges, diary sexps, sub/superscripts, column
groups — and the honest rate over the longer list is 80%.

* DONE Org: statistics cookies ([1/3] and [33%])
=* Project [1/3]= counts the done children. A cookie is text today and
nothing counts behind it, so the number is whatever somebody last
typed — which is worse than absent, because it looks maintained.

The counting is the easy half; keeping it true when a child changes is
a command, and that is where I3 comes in.

Acceptance: =nix develop -c cargo test -p closure-org --test statistics_cookies= and =-p closure-core --test cookie_updates=

Counting in closure-org, staying true in closure-core. It rides on
=set-todo= rather than being a command of its own: a parent's cookie
changing *is* part of finishing the child, so it is one edit and one
undo, and a separate command would let the two drift by exactly the
amount somebody forgot to run it.

Undo needs no stored title. A cookie is derived, so restoring what it
counts restores it — recompute after the reverse and there is nothing
that can go stale if the count changed for another reason first.

Only children with a keyword count: a sub-heading that is not a task
is structure, and counting it would make every project look behind. A
parent with no cookie is left alone, because adding one nobody asked
for edits a title the author wrote.

End to end: =closure set-todo= on a child moved =[1/3]= to =[2/3]=.
Conformance 80% -> 83%.

* DONE Org: #+STARTUP: decides how a file opens
=#+STARTUP: overview= / =content= / =showall= is how a document says
which way it wants to be met. Ignored, so every file opens the same
way whatever it asked for — and =docs/kernel-gpui.org= itself carries
=#+STARTUP: overview=, which closure has been ignoring all session.

Acceptance: =nix develop -c cargo test -p closure-org --test startup=

It does now, and the item cost three things rather than one.

=startup_of= reads the directive (first one wins, blocks skipped) and
=overview= folds at level *one*, not two — folding a headline hides
what is under it, so folding at two folds the children and still shows
them. That was the first bug and the core test caught it.

The second was only visible by looking. The core folded correctly and
the real window still showed every child, because the first design was
a =&mut apply_startup(&shell)= that each shell had to call, and the one
shell anyone looks at did not call it. It is a memo on =&self= now,
consulted by =rows_shared=, so opening a vault applies it whichever
shell did the opening. Its test lives in =closure-shell-gpui= for that
reason: the question was never "can the core fold" but "does opening a
vault fold it". A feature wired to nothing but its own test is the
shape of green that means least.

The third was found by the test written to guard against the second
being half-done: =toggle-fold= could not open a startup fold. It asked
=:VISIBILITY:= whether the row was folded, and a row folded by a
directive has no property — so the first press wrote =folded= onto
something already folded and nothing moved. It takes =Row::folded=
now, which is the state the reader can actually see.

Never a write (I12). Recording =:VISIBILITY:= on every headline
because a file said =overview= would turn opening a document into a
commit, and that property answers a different question: what the
reader last decided, which wins.

Field ceiling: this needed one field to remember the depth, the
=one_struct= ratchet refused it, and the refusal was right — the
outline pane had never been given the cluster the body pane already
had. =OutlineView= (height, wheel offset) came out; ceiling 90 → 89.
The startup depth then turned out to belong in =Memos= rather than in
the cluster, which is why it is not there.

Conformance 83% → 85% on both numbers, floors raised.

* DONE Gates: the local gate is weaker than CI, and CI is red
=just gates= runs clippy three times and none of the three passes
=-D warnings=. =nix flake check=, which is all CI runs, uses
=clippy --workspace --all-targets --locked -- -D warnings=. So the
command the loop treats as the gate exits 0 on a tree that CI rejects,
and the justfile's own comment says exactly why that is not allowed:
"a gate that differs from CI is a gate that lets CI fail for a reason
nobody ran into locally".

It is not hypothetical. Forty distinct warning sites are outstanding
right now, every one of them in code written this session, including
three constants — =PANE_ROW_H=, =PANE_ROW_PAD=, =PANE_WRAP_GUESS= —
that are dead: leftovers from the pane-height attempt that no longer
has a caller. Dead constants in the file that guesses at row heights
are the worst possible thing to leave lying there, because the next
person to open that item will read them as the current design.

My own fault twice over: I have been grepping gate logs for
=^warning: (unused|missing|unre|dead)= all session, which is a pattern
narrow enough to miss most of clippy, and I never once checked what CI
demanded that the local gate did not.

Acceptance: =nix develop -c just gates= exits 0 with =-D warnings= on
every clippy line, and =grep -c "^warning" = on its output prints 0.

All three clippy lines carry =-D warnings= now, and the forty sites are
gone. Thirty-odd were mechanical. Six were not:

The three "dead" constants were not dead. =PANE_ROW_H=, =PANE_ROW_PAD=
and =PANE_WRAP_GUESS= have callers; the callers sit behind
=#[cfg(feature = "gpui")]= and the constants did not, so the *default*
build saw them unused. Deleting them — which is what "never used" reads
as, and what I was about to do — would have broken the window. They are
gated like their callers now.

Two clippy suggestions were refused with a reason in the source rather
than applied: merging =Column::Property= and =Column::Relation= into one
match arm (they read the same *without a vault*, which is the thing the
comment beside them explains, and one arm has nowhere to put that), and
=map_or_else= over a twenty-line brace-counting scan.

Seven functions were over the hundred-line limit, and every one of them
had a real seam rather than needing an allow: =command_pick_rows= (the
palette and describe-command built the same rows two arms apart, one
writing the separator escaped and the other as the character),
=block_pick_rows=, =pick_listed=, =preempt= (the two lookups that run
before any surface sees a key — a concept the comments already named),
=row_plus=, =windowed_rows= (the manual and the journal spelled out the
same seven arguments), and =begin_frame= (sixty lines of per-frame
housekeeping that ran before anything was painted). Every one of those
names a thing that was already there and had not been said.

The window was re-run after the refactor and paints identically.

* DONE Org: a timestamp with a time range is a span, not a date
=<2026-08-12 Wed 10:00-11:00>= is how org writes "this meeting runs
from ten to eleven", and it is the single most common thing on a
calendar. =find_timestamps= returns its content as a string and
nothing reads the =-11:00=, so a one-hour meeting and an all-day
"sometime Wednesday" are the same value to everything downstream: the
agenda sorts them together, and no shell can say how long anything is.

The parts are already here and unconnected. =stamp_minutes= turns an
=HH:MM= tail into minutes since midnight, and =stamp_days= differences
two dates; they were written for =CLOCK:= lines and know nothing about
=TimestampView=. This wants the reading, not new arithmetic.

Acceptance: =nix develop -c cargo test -p closure-org --test time_range=

=span_of= reads a stamp's content into a =TimeSpan=: the date, the
minute it starts, the minute it ends. =minutes()= is =None= for a
stamp with no end *and* for =11:00-10:00= — org does not say what a
backwards range means, and a guess (zero, or a wrap into tomorrow)
would put a made-up number into an agenda.

Two things the tests pinned that a looser reading would have got wrong.
=<... 10:00-11:00 +1w>= is a weekly meeting, and a "last word" rule
reads =+1w= as the end time; the time is found as the one word
containing a colon instead. And a stamp with a *malformed* time still
comes back with its date rather than as nothing: the day is information
the file really carries, and dropping it loses more than the bad time
did (I5).

Offered by the agenda, and this is where looking paid again. The first
version taught =agenda_rows=, the order in the window came out right —
nine o'clock above four o'clock, where before it was alphabetical — and
the pane showed no times at all, because the window paints from
=agenda_context=, a second path to the same list. Both carry the time
now and a test asserts they agree, since two paths to one list is the
shape that lets them drift.

Then the column was 44px and =09:00= wrapped onto two lines, making a
timed entry twice as tall as an untimed one. 62px and =whitespace_nowrap=.
Screenshot verified: SCHEDULED / 09:00 / Stand-up, then 16:00 / Review
the queue, then a blank column for "Errand, sometime" — an entry with
no time is shown with none rather than 00:00.

Sorting is day, then whether it has a time at all, then the time, then
title. A timed entry sorts ahead of an untimed one on the same day:
something at nine is a thing you have to be somewhere for.

Conformance 85% → 88% on both numbers, floors raised.

* DONE Org: #+BEGIN: clocktable reports the time you clocked
=CLOCK:= lines are parsed, =Vault::clock_minutes= sums them per
headline, and the CLI has =clock-report=. The one place a reader would
look for the answer — a =#+BEGIN: clocktable= block in the file the
work is written in — is empty. Every part exists and the block is not
connected to any of them.

The interesting question is what "filled in" means here. Org writes the
table into the file, so a document's bytes depend on when it was last
refreshed. closure cannot do that (I12, and I1 behind it): a clocktable
is a *view*, rendered where the block sits and never written back. Same
resolution as =#+BEGIN: closure-widget=, and worth stating in the item
because "org does X" is the argument that would otherwise win.

Acceptance: =nix develop -c cargo test -p closure-org --test clocktable=

=clocktable_params= reads the block's options (=:maxlevel=, =:scope
file|vault=) and =render_clocktable= turns the rows =Vault::clock_minutes=
already produced into an org table. Durations are =H:MM= — org's own
format, not =65m= and not =1.08h=, because a file read by closure and
by Emacs has to agree what the number means. Nothing clocked reads "no
clocked time" rather than a table with no rows under it, which looks
like a broken report.

The block's *existing body* is skipped and replaced rather than parsed.
Whatever is between =#+BEGIN:= and =#+END:= is what some previous
refresh left there — possibly an Emacs's — and is not an input to the
answer.

Rendered through the same preview seam as widgets, entities, includes
and footnotes, and the screenshot is the clearest picture of I12 in the
session: the detail pane shows six lines of table while the meta line
above it says =2 lines=, because the file still holds exactly the
=#+BEGIN:= / =#+END:= pair the author typed. Org fills a clocktable in
by rewriting the document; a document whose bytes depend on when it was
last refreshed is not one you can diff, sync or roundtrip.

The gate caught three warnings in this code as it was written — a
collapsible =if=, two =format!= appends and an unused =self= — which is
the first time the =-D warnings= change paid for itself. Clippy's fix
for the first would have put a side-effecting iterator call in a match
guard; written out instead, with the reason.

Conformance 88% → 90% on both numbers, floors raised.

* DONE Org: subscript and superscript are markup, not three characters
=x^2= and =H_{2}O= were preserved and never read, which in a notebook
that holds maths, chemistry or units is the difference between a
formula and a typo.

Acceptance: =nix develop -c cargo test -p closure-org --test scripts=

The whole difficulty is in what is *not* one. =foo_bar= is a variable
name and a vault is full of them — source blocks, file names, property
keys — so a marker is a script only when what precedes it is
alphanumeric and what follows is a brace group or an alphanumeric run
starting with a digit. That is org's rule and it is also the only rule
that leaves =snake_case= alone.

One test premise of mine was wrong and the code was right: =H_2O=
subscripts ="2O"=, not ="2"=, because org takes the whole word. That
is why chemistry gets written =H_{2}O=. Copying my assumption into
closure would have made the same file render two ways in two editors,
so the test was corrected and the real rule written down beside it.

Offered as Unicode where Unicode has a form, and all-or-nothing: a
script with one unrenderable character keeps its source entirely. The
screenshot shows all three rules on three consecutive lines — =a² + b²
= c²= and =H₂O= rendered, =n^{th}= left alone because there is no
superscript =t=, and =$x^2 + y^2$= left alone because it is maths.

That last one was a bug the shipped =entity_preview= test caught: the
first version rendered inside LaTeX and produced =$x²$=, which is
neither the source nor maths. A fragment is something to render whole,
not a string to substitute inside. Now stated as its own test rather
than left as a thing that happens to hold.

Conformance 90% → 92% on both numbers, floors raised.

* DONE Org: |<r>| aligns a column
Alignment rows were kept and never applied, so every table came out
left-aligned whatever it asked for. In a column of durations or amounts
that is the difference between something you can scan and a ragged wall
of digits, which is the whole reason org has the row.

Acceptance: =nix develop -c cargo test -p closure-org --test column_align=

The row is a *directive shaped like a table row*, and the case that
decides whether the feature is safe is the negative one: a data row read
as a directive makes a table's first line vanish. A cell must be exactly
=<r>=, =<l>= or =<c>=, optionally with org's width cookie, or empty —
anything else and the whole row is data.

The width is deliberately dropped. It is a hint for a fixed-width Emacs
buffer, and a shell laying out its own columns has a better number than
one somebody typed years ago.

=render_table_aligned= takes the alignments and =render_table= is now it
with none, so every existing caller and every golden file keeps its
answer. The header stays left-aligned even over a right-aligned column:
a right-aligned heading over right-aligned numbers reads as part of the
numbers.

Offered by re-laying the table in the preview and dropping the directive
— it is an instruction, not data, and printing it back made the reader
do the parsing. I checked partway through that a test in =closure-query=
is not a shell offering anything, and moved the claim to the shell test
rather than leave the matrix flattering itself.

Conformance 92% → 95% on both numbers, floors raised. The two left —
=#+ATTR_*= (export-only) and diary sexps (needs an elisp evaluator) —
are the ones that should stay Preserved, and =the_matrix_is_worth_having=
is there to fire if I ever call this finished.

* DONE Bug: a clocktable ignores its own :scope, and the default is wrong
Written by me two commits ago. =clocktable_params= reads =:scope
file|vault= and the preview binds it to =_params= — the underscore is
the compiler being told to stop mentioning the one thing the feature
needs — then reports =Vault::clock_minutes()=, which is the whole vault
every time.

So the *default* is wrong, not just the option: a block that says
nothing means =:scope file= in org and in closure's own parser, and it
reports the vault. A weekly note in a vault of fifty files shows fifty
files' time under the heading "this week".

Exactly the "parsed and never read" shape the conformance work spent
the session removing, reintroduced by the person removing it. Worth
having in the queue in its own words rather than quietly amended into
the commit that caused it.

Acceptance: =nix develop -c cargo test -p closure-shell-core --test clocktable_scope=

=Vault::clock_minutes_in(Option<&Path>)= does the scoped sum and
=clock_minutes()= is it with =None=, so nothing that already called it
changes answer. The preview passes the file the headline is in, which
was already sitting in a local two lines above the call — the
information was there and the binding threw it away.

Verified with a two-file vault on :1: the block in =notes.org= reports
that file's 1:30 and 0:45 and not the 2:00 in =other.org=.

The lesson is the underscore. =let Some(_params) = ...= compiles, reads
as deliberate, and is the compiler being told to stop mentioning the
one thing the feature needs. Nothing in the gate can see it — clippy is
satisfied, the tests I had written passed, and the screenshot I took at
the time was of a single-file vault, which is the one shape where the
bug is invisible.

* DONE Org: the five constructs the honest matrix just admitted to
Added to =CONFORMANCE= while declining the other two, because a matrix
with nothing Preserved is a matrix that stopped looking. They are real
and they are small:

- [X] =#+PROPERTY: header-args :tangle no= — done. =document_properties=
  reads them, keys upper-cased so =#+PROPERTY: header-args= and
  =:HEADER-ARGS:= are the same property; a directive quoted inside an
  example block is not a default, the rule =#+STARTUP:= already
  follows. =effective_properties= puts the drawer first and the file's
  defaults behind it, and marks the inherited ones — "delete this line
  to change it" and "change the line at the top of the file" are
  different instructions. The window prints =(from #+PROPERTY:)= beside
  them. Never written into a drawer (I12): opening a file must not be a
  commit. Conformance 88% → 91%, understood floor 40 → 41.
- [X] =#+LINK: gh https://github.com/%s= — done. Two substitution
  forms, both org's: =%s= puts the tail where it is written, a template
  without one gets it appended (the shape somebody writes first). The
  case that decides whether this is safe to run on every link is the
  negative one — =KNOWN_SCHEMES= means a file cannot break =https:= or
  =id:= by declaring them, which is the one way the feature could do
  harm. =abbreviated_link_rows= shows where each one goes and lists
  only the abbreviated ones: for a link that already says, repeating it
  is noise. Body keeps the short form (I12) — being able to write it is
  the point. Conformance 93% → 95%, understood floor 42 → 43.
- [X] description lists (=term :: definition=) — done. The spaces in
  = :: = are the whole rule: =std::collections= and =foo::bar= are
  everywhere in a vault that holds code notes, and a separator without
  them would file half of those lines in a glossary. First separator
  wins, so a term may contain =::= itself. A term with nothing after it
  is not one — guessing an empty definition enters a word as meaning
  nothing. Offered as =glossary_rows=: vault-wide and sorted, because a
  glossary you can only see by standing on the note that defines the
  word answers a question nobody has, and sorted is the difference
  between a list you read and one you look something up in.
  Conformance 95% → 97%, understood floor 43 → 44.
- [X] =[@3]= counter override — done. =[ ]=, =[X]= and =[-]= sit in
  exactly that position and a vault is full of them, so the =@= is what
  tells a counter from a checkbox. The items *after* it follow on: a
  counter that renumbered only its own line would leave "3." then "2.",
  which is worse than not reading it at all. The run ends at the first
  line that is not an ordered item, so a later list does not inherit a
  number it never asked for. Window shows =3.= / =4.= with the cookie
  gone; the file keeps =1. [@3]= (I12).

All five done. The matrix then read 100% understood with nothing
Preserved, and =the_matrix_is_worth_having= fired for the second time
this run — correctly, because org has more than 47 constructs. Six more
are listed now: =#+CAPTION:=, =[[*Headline]]= search links,
=#+PRIORITIES:=, inline footnotes =[fn::text]=, LOGBOOK state-change
notes, and =remote()= table references.

That took the ratio 97% → 88% while the understood *count* went 40 → 45.
Which is exactly the case the count floor was added for: the ratio fell
because closure admitted to more of org, and the number that cannot be
gamed by growing the list went up by five.
- [X] timestamp delay (=-2d=) — done, and it was two features. There
  was no warning period at all: =agenda_until= kept entries whose date
  was =<= today=, so a DEADLINE surfaced on the day it was due and not
  a moment before — the one moment a warning has stopped being useful.
  The cooldown was not merely unread, it had nothing to narrow.
  =DEADLINE_WARNING_DAYS= is 14, org's default, so a vault read by both
  agrees about what is on today's list; =delay_of= narrows it per
  timestamp. Kept apart from =parse_repeater= on purpose: both are a
  sign and a unit in the same brackets and =<... +1w -2d>= is both at
  once. The =:= guard is what keeps =10:00-11:00= from reading as an
  eleven-unit warning. SCHEDULED is untouched — a plan appears on its
  day, which is what makes it a plan. The agenda says =tomorrow= in the
  warning colour and =in 8 days= muted, because otherwise listing
  deadlines early only makes the agenda longer.
  Conformance 91% → 93%, understood floor 41 → 42.

Split into one item each if any turns out to be more than a commit.
Each needs a shell to offer it before the matrix may claim it.

Acceptance: =nix develop -c cargo test -p closure-org --test property_inheritance --test link_abbrev --test description_lists --test list_counter --test timestamp_delay=

* DONE Coverage: the target and the freeze contradict each other
Measured before splitting anything: 86.32% lines, 7,162 missed of
52,370. The goal asks for 100. Where the missing lines actually live:

| crate                   | missed |  total | in scope? |
|-------------------------+--------+--------+-----------|
| closure-cli/main.rs     |   1697 |   2721 | barely    |
| closure-shell-core      |   1407 |  14985 | yes       |
| closure-tui             |   1279 |   3316 | FROZEN    |
| closure-org             |   1044 |   9659 | yes       |
| closure-store           |    478 |   9602 | yes       |
| closure-core            |    305 |   1640 | yes       |
| closure-lsp             |    150 |   1034 | FROZEN    |
| closure-eval            |    144 |    643 | yes       |
| closure-llm             |    135 |    516 | yes       |
| closure-shell-gpui      |    102 |   1010 | yes       |

About 3,300 of the 7,162 — 46% — are in crates this goal freezes or
all but freezes. So 100% is not reachable inside the stated scope, and
saying that out loud is the item.

The resolution, unless the reporter says otherwise: *a test is not a
feature*. "Frozen — must keep compiling, gains no features" permits
adding tests to a frozen crate and forbids adding behaviour. Every
coverage item below works under that reading, and no frozen crate gains
a single line of non-test code.

The second half is harder and is why this item exists rather than just
a number. The goal forbids exclusions, =#[coverage(off)]= and gaming.
Some lines genuinely cannot be executed by a test — a =main()= that
needs a display, an =Err= arm for a syscall that cannot fail in a
sandbox. Under those rules the only honest move for such a line is to
*delete or restructure it*, not annotate it. That will shrink the CLI
and the shell =main.rs= files, and it should.

Acceptance: this item's body records the decision, and =just coverage=
names the target it is ratcheting toward.

* TODO Coverage: closure-cli is 37.6% covered
1,697 missed lines of 2,721 — the single largest hole in the workspace,
and the lowest-covered file in it by a wide margin.

=closure-cli/src/main.rs= is 5,684 regions and 497 functions in one
file. It is a command surface: most of those lines are subcommand arms
that no test ever runs. That is also why it is cheap to fix — an arm
that takes a vault and prints is the easiest thing in the repo to test.

Expect this to delete code as well as cover it. A subcommand nobody can
reach and nobody tests is not a feature.

Acceptance: =nix develop -c cargo llvm-cov -p closure-cli --fail-under-lines 95=

37.6% → 51.9% (1,311 missed, was 1,697) from one table-driven test over
the read-only subcommands. The arithmetic was the whole story: 141
subcommands against 41 tests, so most of those lines were arms nobody
ever ran.

What it asserts is deliberately weak and broad — exits without panicking
— which is not a claim about *what* a command printed. That belongs in
a test per command. It is the claim that the arm works at all, which
nothing was making.

Three passes, and the second and third are the ones that find things: a
full vault, an *empty* one, and a vault directory that is not there.
Queries written against content usually work; the same query against
nothing is where =.first().unwrap()= and "divide by the number of
headlines" live.

Still short of 95. What remains is the commands that *write* — capture,
rename, set-todo, promote, archive, move — which need a fixture per
command and an assertion about the file afterwards, and the ones that
need a network or a subprocess. 51.9% → 57.5% with the writing half. Each case holds three claims,
because "exits 0" is a much weaker thing to say about a command whose
job is to change a document: it succeeds, the file *changed*, and the
vault still passes =check= — a rewrite closure cannot read back is the
failure that loses notes rather than merely annoying somebody.

The case that would catch a panic is the id that is not there, since
every one of these looks an id up and a typo is the ordinary way to get
one. It also asserts the file was left alone, which a non-zero exit does
not promise.

=toggle-checkbox= takes the Nth *preamble* list item, not one in a body.
My fixture had the checkbox under a headline and the command said "no
headline at the given path" — a true message about a question I was not
asking.

Still short of 95. What is left needs a network, a subprocess, or a
terminal: =serve=, =sync=, =tui=, =gpui=, =egui=, =mcp=, =acp=, =lsp=,
=plugin=, =pkg=. Those are the ones the coverage parent item says must
be deleted or restructured rather than annotated if they cannot be
reached — worth deciding one at a time rather than in a batch.

57.5% → 65.9% with the commands that take an argument beyond the vault
or the file — an id, a needle, a path, an index — which was most of what
remained. Two claims each, and the second is the one worth having: it
works on a real fixture, and it *fails* rather than panicking when the
argument names something absent. Every one of these looks something up,
and a wrong id is the ordinary typo.

Three assert what the command is actually for rather than that it ran:
org → md → org has to come back parseable, =tangle= has to write the
file its =:tangle= header names, and =check-config= has to *reject* a
bad config — a checker that accepts everything is not one.

65.9% → 68.0%, and the "needs a network or a terminal" list turned out
to be mostly wrong. =mcp=, =acp= and =lsp= read requests from *stdin*,
so a closed stdin is a complete hermetic session: they must start,
serve nothing and exit. That is a real claim — a server that hangs on
EOF hangs the editor that launched it — and it was untested.

Writing those found the same defect =config.org= had, in a second
place: both cron commands searched =doc.preamble()=, so a schedule
written under =* Jobs= was invisible to the CLI while the shell's cron
pane showed it. One fact, two owners. They go through
=closure_shell_core::segment_body= now, which is the reader the pane
already uses, so the two cannot disagree about a file again.

What is genuinely left needs a socket or a terminal: =serve=, =sync=,
=tui=, =gpui=, =egui=, =plugin=, =pkg=, =ask=, =chat=.

* DONE Does the SPC leader engage on the kernel API as it does in the window?
Doom binds both =q= → quit and =SPC q r= → reload-shell, so only the
leader state tells them apart. Fed as three =on_key= calls (=SPC=, =q=,
=r=) the app ends up with =should_quit= set — so the =q= was taken as
the standalone binding rather than as the second stroke of a chord.

Two possibilities and I do not know which:

- the chord trie does not engage through =ModalApp::on_key= the way it
  does through the window, which would be a real bug and a nasty one
  (=SPC q r= quits instead of reloading);
- or =every_chord_in_the_kernel.rs='s harness does not feed strokes the
  way the window does — wrong =text=, or a mode the window sets first.

The window path demonstrably works: =settings_and_chords.rs= presses
=g= and the which-key popup lists =wk-chord-agenda=, so prefixes
resolve there.

Until this is answered, =escape_gets_back_out_of_whatever_a_chord_opened=
asserts only single-stroke chords, which is stated in the test.

Acceptance: a test that =SPC q r= reloads and does not quit — or, if the
harness was wrong, the multi-stroke assertion restored with the fix to
how strokes are fed.

The harness was wrong. =modal_stroke= maps the key *named* ="space"= to
=SPC=; my encoder sent a literal =" "=, which falls through to the text
branch and becomes a stroke called =" "=. So =SPC q r= never matched
anything, the pending chord cleared, and the =q= was taken as the
standalone quit binding.

The trie is correct and always was: push the stroke, try the whole
chord as an exact match, then as a prefix, and clear only if it is
neither.

Two tests now say so directly rather than leaving it to the sweep —
=SPC q r= reloads and does not quit, and =q= on its own still quits. A
leader that swallowed =q= everywhere would make the chord work and the
plain key dead, and only asserting one of those would miss it.

The multi-stroke assertion is restored, so the sweep covers every chord
in the keymap rather than the single-stroke half.

* TODO Coverage: closure-shell-core, 1,407 missed
Of 14,985 lines, so 90.6% — the absolute number is large because the
crate is. Likely concentrated in surfaces the tests reach through
=ModalApp= but never in their error arms.

Acceptance: =nix develop -c cargo llvm-cov -p closure-shell-core --fail-under-lines 97=

89.5% → 90.4% by pressing every chord of every mode through =on_key= on
four vaults: full, an empty file, no files at all, and a bare headline
with no body, properties or id. The last three are the ones that find
things — an arm that indexes the selected headline is fine until there
is not one.

Each chord gets a fresh app, so a chord cannot be judged by what the
previous one left behind, and the assertion beyond "no panic" is that
the cursor is still within its own row list: a command that leaves it
past the end is a panic on the *next* frame rather than this one.

Also turned up a question worth its own item — see above — about
whether the leader engages through the kernel API the way it does
through the window.

* TODO Coverage: closure-org, 1,044 missed
Of 9,659, so 89.2%. This is the kernel's parser and the one crate where
an uncovered line is most likely to be a real unreached branch rather
than a plumbing arm — I5 says no panics here, and a branch no test
enters is a branch nobody has checked cannot panic.

Acceptance: =nix develop -c cargo llvm-cov -p closure-org --fail-under-lines 97=

A corpus of documents that are legal org and awkward to parse, each one
something somebody actually writes rather than fuzz: CRLF, a byte-order
mark, tabs for indent, seven-star headlines, an escaped =,*=, a drawer
never closed, a block never closed, an =#+END_SRC= with no beginning,
nested blocks, a colon inside a property value, a Windows path in one,
tags with no title, a ragged table, mixed list markers, a footnote with
no definition and a definition with no reference, a link with brackets
inside it, RTL text, emoji, and a hundred-thousand-character line.

Every one roundtrips byte-exact through =source()= *and* through
=print()= — two different code paths, one returning what was read and
one re-serialising — and every one is walked by every whole-document
accessor and every free function a shell runs over a body, because I5
says the kernel does not panic and this is where that gets tested
cheaply.

All of them pass, which is the useful finding: the parser is robust to
things I expected to break it.

One correction to my own test before believing it: the free-function
half originally looped over the corpus inside the outer loop and broke
after one iteration, so every one of those functions saw only the empty
string. Coverage that looks broad and is not.

* TODO Coverage: closure-tui is frozen and holds 1,279 missed lines
38.6% of the crate is untested. Under the reading in the parent item a
test is not a feature, so this is allowed and is the largest frozen-
crate hole.

Worth doing before the smaller in-scope ones, because if the reading is
wrong this is where it will show, and it is better to find that out
against the biggest case than the smallest.

Acceptance: =nix develop -c cargo llvm-cov -p closure-tui --fail-under-lines 90=

The bulk of what was left is =draw=, and it did not need a terminal at
all — it needed a =pub=. ratatui ships =TestBackend=, a buffer you draw
into and read back, and =draw= was private and therefore reachable only
from a loop that wants a TTY. That is the restructuring the parent item
asks for and it cost one keyword.

Every surface a chord opens is now rendered at four sizes: 80x24,
200x60, 20x5 and 1x1. The absurd ones are the point — a render that
divides by a width or subtracts a border has its bug at the small end,
and it shows up on somebody's machine at whatever size their terminal
happens to be.

Beyond "did not panic", the browse surface has to actually contain the
headline it opened.

Every chord the keymap defines, in all five modes, against three shapes
of vault — full, empty, and no files at all (=App::new(Vec::new())= is
a shipped case with its own test, so every chord has to survive it).
Plus every chord pressed *twice*, because the second press lands in
whatever surface the first opened, which for most of them is the only
visitor that mode will ever get.

The reading holds: a test is not a feature, and closure-tui gained only
tests.

=escape_gets_back_out_of_every_surface= is the one with a claim in it —
whatever a chord opened, Escape leaves — and it is the property that
makes a modal shell usable rather than a thing you kill the terminal to
escape.

It also cost a red run for the right reason: this shell's stroke
vocabulary is the terminal's (=RET=, =ESC=, =C-c=) and the gpui
window's is gpui's (=enter=, =escape=). The chord *table* is shared
between them, which is exactly what makes the difference easy to
forget, so it is stated in the test now.

Workspace coverage 86.32% → 87.62%; =just coverage= floor 86 → 87.

* TODO Coverage: the tail — store, core, eval, llm, gpui, lsp
478 + 305 + 144 + 135 + 102 + 150 = 1,314 missed across six crates,
none individually large. Split further only if one of them turns out to
be hiding something.

Acceptance: each of the six passes
=nix develop -c cargo llvm-cov -p <crate> --fail-under-lines 95=

closure-store next, and the tests are about the edges rather than the
happy path, which is thoroughly covered already: a directory with no
org in it, a nested one, a file that is not UTF-8, an id that is not
there, and the statistics on an empty vault — every one of those
divides by something, and empty is what makes the something zero.

Four are about not losing data, which is what the store is for:
=create_file= must not overwrite an existing note (the gesture is "new
note", and silently replacing one is the worst reading of it),
=rename_file= must not clobber its target, =delete_file= on a missing
path must be an error rather than a panic, and =save= must leave a file
that parses — a half-written file is worse than an unsaved one, because
the unsaved one is still in the editor.

All twelve pass.

closure-core next, and asserting I3 across *every* command rather than
one at a time — apply, then undo, then require the source back to the
byte — found two bugs that lose data.

=set-body= undo duplicated the planning line. =current_body= captured
"the body" by concatenating the headline's body *nodes*, and
=rewrite_headline_set_body= replaces from the end of the property
drawer — but a =SCHEDULED:= line is a body node sitting *above* that
drawer. So it was inside what was captured and outside what was
replaced, and undo wrote it back a second time. One fact, two owners,
which is this codebase's recurring shape. =headline_body_text= is the
one owner now and lives beside the rewriter so they cannot drift.

=set-property= could not be undone at all when the property was new:
the reverse returned =Err= for =old: None= instead of *removing* the
property. The right reasoning was already written down where
=:LAST_REPEAT:= is reversed — "a property that was not there before is
cleared rather than set to an empty string, so undo leaves the drawer
as it found it" — and applied in that one place only.

Both are the kind that surface three edits later as a file that no
longer roundtrips, by which time nobody can say which command did it.

closure-eval first, and writing the tests found a hang rather than a
gap. =Bounds= is what stands between a vault and a =while true= in
somebody's note, and =run_bounded= enforced neither half against a
child that outran the reader: =child.kill()= reaches =/bin/sh= and
nothing it started, so a block running =yes= left the grandchild
writing into the pipe the drain thread was reading, and the =join=
below waited forever. A five-second bound ran past ten minutes.

The codebase already knew. The spawn sets =process_group(0)= "so a
future group-kill can reach its descendants", and =shell_escape='s doc
comment says outright that =run_bounded= "looks like it covers this and
does not". Both halves were written down and neither was applied here.

Fixed by killing the group rather than the child, and by giving up on a
drain thread instead of joining it — the survivor holds nothing but a
pipe and its own buffer. Ten tests, 0.2s, no stray processes.

A note on measuring: =cargo llvm-cov -p <crate>= runs only that crate's
tests, so its number is not the workspace number the parent item lists.
Compare like with like or the ratchet will look like it moved when it
did not.

* TODO Coverage: raise the workspace gate to 100 and keep it there
The last item of the group. =just coverage= currently reads
=--fail-under-lines 86=; it becomes 100 when the crates above are done,
and =just gates= gains the coverage line so a drop fails the same
command everything else does.

Acceptance: =nix develop -c just coverage= exits 0 at
=--fail-under-lines 100=.

* DONE Sync: IrohTransport is a Transport that always fails
=impl Transport for IrohTransport= exists and both =push= and =pull=
return =Err("iroh/ipfs push not implemented in stub")=. A type that
satisfies a trait by erroring is worse than an absent one: it type-
checks, it is constructible, it appears in the shells matrix, and it
tells the caller nothing until runtime.

The goal that seeded this said "iroh/IPFS is a comment". It is not — it
is a lying implementation, which is the more expensive kind.

Decide and do one: implement it over real iroh, or delete the type and
say in =docs/spec.md= that P2P is out of scope for v1 and =Transport=
has exactly the impls that work. Do not leave a third option where it
keeps compiling and keeps failing.

Acceptance: =grep -c "not implemented in stub" crates/closure-sync/src/lib.rs=
prints 0.

Took the second option: removed, not implemented. The reasoning is in
the source where somebody reaching for a P2P transport will find it.

Implementing it is not the missing piece. =TcpSyncTransport= already
moves real frames over a real socket to any =SocketAddr=, and the
sessions, the merge and the ed25519 signing above it are real. What P2P
adds is *discovery* — two laptops behind different routers have no
address to type — and that is the next item, where the design question
belongs. Shipping a transport now would have answered it by accident.

Its test could not fail either: it asserted the error mentioned "iroh",
which the stub's own message did, so it passed whether or not the binary
was present. Rewritten in place, dated, with the reason — what replaces
it is the property that was actually worth holding: no type in this
crate satisfies =Transport= by erroring.

=Transport= now has exactly the impls that work, =NoopTransport= and
=GitTransport=. The no-op is honest and says so in its name; that is a
different thing from a type that claims a protocol and returns an error.

* DONE Sync: two peers that cannot already reach each other cannot sync
Correcting my own assessment before acting on it: the wire protocol is
real. =SyncMessage= framing, =SyncSession=, merge, and ed25519 signing
all work, and =TcpSyncTransport= moves real bytes over a real socket
with =connect_and_sync= / =serve_once=. It is not restricted to
loopback — loopback is just where the tests run it, which is correct.

So the gap is not "no transport". It is that one peer must already know
a =SocketAddr= the other is listening on. No discovery, no NAT
traversal, no relay. On two laptops behind different routers — the case
"P2P sync" is *for* — there is no address to type.

Acceptance: a test that two sessions converge knowing only a shared
identifier, never a =SocketAddr=:
=nix develop -c cargo test -p closure-sync --test discovery=

=announce= / =discover= over a multicast group of closure's own, keyed
by a name the two people already share. What comes back is the
=SyncTicket= pairing already takes, so this fills in what somebody would
otherwise paste rather than adding a second way to pair.

The acceptance said "a test that two sessions converge knowing only a
shared identifier". That test does not exist and cannot on this
machine: =ip -o link= shows =lo= without a MULTICAST flag, so nothing
addressed to a group comes back and an integration test would pass only
somewhere else. Rather than write one that is green by accident of
environment, the protocol was split out — =announcement_line= and
=parse_announcement= are pure and carry every decision — and the socket
around them is thin. Six tests cover the format, the name match, the
version gate, noise on a shared group, and the round-trip.

The parsing detail that matters for safety rather than tidiness: a
vault named =a b= must not announce as one named =a=. The name is the
only authorisation at this stage.

Across routers it does not work, and that is a decision in
=docs/spec.md=: a relay or STUN server is a service closure would have
to run, which changes what closure is. The two honest options today are
a VPN that makes the machines local to each other, or =GitTransport=
through a repository both can reach.

* DONE A2A: swarm_drain is one worker in a trenchcoat
=pub fn swarm_drain(vault, _num_workers: usize, task_ids)= — the
underscore is the parameter that names the feature. The body is a
=for= loop with a =HashSet= claim, and the doc says so honestly ("N is
advisory (sim is sequential for hermetic test; real swarm uses threads
+ locking or CRDT claim)").

Honest docs on a stub still leave =closure-a2a= claiming, in its own
module header, that closure "participates in agent swarms as a
first-class peer".

Either make N real — threads, and the claim through the CRDT so two
workers racing for one task is a merge and not a lock — or rename the
function to what it is and correct the header. The exactly-once
property is the thing that must survive whichever way this goes.

Acceptance: =nix develop -c cargo test -p closure-a2a --test swarm=

Took the second option — correct the claim — because =closure-a2a= is
frozen for this run and real concurrency is a feature. The reasoning is
in the source, not just here.

The bigger find was the test. It named "every task done exactly once"
and asserted it with =prop_assert!(true)= after an empty =for= loop,
under a comment admitting the cross-check was not done. Its only real
assertion was =drained == unique.len()=, and =drained= is the function
counting its own inserts — so the test could not have failed for the
reason it existed. Rewritten in place, dated, with the reason.

Exactly-once now means a claim about the *vault*: every id carries the
mark, none twice, none missed — including an id the queue lists twice,
which is two producers and one task. The property holds. That is worth
saying plainly, because partway through I had it failing and wrote that
the mark never lands; the mark lands, and my test was looking for
=SWARM-STATE= where the drain writes =swarm-state=. My bug, not the
product's.

=the_worker_count_is_not_used= states the gap as a test rather than a
doc comment, since the signature says otherwise and a reader is
entitled to know. One worker and a hundred produce identical vaults.
It is also the test that has to change on the day the drain becomes
concurrent, and it will be obvious why.

* DONE Config: a new key can arrive with no constraint and nothing notices
Correcting the seed again: config validation is *not* per-key
=parse_bool=. =Config::load= already rejects unknown keys with a
=nearest_key= suggestion and a line number, and enforces real cross-key
constraints — an =llm_endpoint= with no =llm_provider=, a BYOK provider
with no =llm_key_env=, =openai-compatible= with nowhere to point. Those
are good errors and they arrive at load (I9).

The gap is that they are hand-written =if= statements in one long
function. Nothing relates a key to its constraints, so a key added
tomorrow is unconstrained by default and no test says so. CUE's actual
value is that constraints are *data*: composable, enumerable,
reportable.

Make the constraints enumerable and add the ratchet: every field of
=Config= either names a constraint or is explicitly marked
unconstrained, and a new field that does neither fails the build. That
is the same shape as =one_struct= and the conformance matrix, both of
which have earned their place this session.

Acceptance: =nix develop -c cargo test -p closure-config --test every_key_is_constrained=

=KEY_RULES= is the table: =(key, ValueKind, requires)=, one row per
key, and =check_value= runs it *before* the parsing arm — so it is what
executes rather than a second list agreeing by coincidence. Nine tests
hold it: every key has a rule, every rule names a key, nothing twice,
every =requires= names a real key, and =Free= stays a minority so the
table keeps meaning something.

Two real bugs fell out, neither of which was the thing I set out to fix.

=outline_width = wide= was accepted in silence. The arm parsed with
=.ok()= and kept the default, so a hand-typed width did nothing and
said nothing — precisely the failure I9 exists to prevent, in the one
key whose arm had no error path. It is =PositiveInt= now and says so
with a line number.

=log_done=, =outline_width= and =recent_files= worked and were absent
from =known_keys()=, because that list is derived from the shipped
default config and they were never written into it. =nearest_key= could
not suggest them and no user could discover them. Three working
settings nobody could find. They are documented now, which is also what
made the ratchet pass rather than the other way round.

The cross-key checks moved into the table's =requires=, so the next one
is a field rather than another =if= somebody has to remember to add.
The two that stayed hand-written are genuinely conditional — BYOK needs
a key env *unless* the provider is keyless — and a table that could
express that would be the constraint language this deliberately is not.

Not CUE. A constraint *language* would be a second thing to learn for a
file with twenty-eight keys. What CUE is actually for here is that
constraints are data you can enumerate, test for completeness and
print, and that is what this is.

* DONE Bug: a config error never reaches the gpui window
=load_reporting= returns the complaint, =run()= binds it, and the
comment above the call says "Every reader of this file used to drop the
error on the floor... It reaches the status line now."

It does not. A vault whose =config.org= says =outline_width = wide=
opens with no message anywhere in the window — checked at 3s and at 6s,
whole window, not a crop. The setting is silently ignored and the
reader is told nothing, which is the exact state that comment was
written to end.

Either =set_status= at construction is overwritten before the first
frame absorbs it, or the toast expires unseen. A startup complaint
should not be a toast at all: it is true until the file is fixed.

The shipped =config_errors_reach_you.rs= does not catch this because it
asserts on the error *string*, not on a shell showing it — the
understood-versus-offered gap one layer up from org. Whatever fixes
this needs a test that fails when no shell displays it.

Acceptance: =nix develop -c cargo test -p closure-shell-gpui --features gpui-test --test config_complaint=

The title of this item is the symptom and it was not the bug. The
window's status path was fine all along — =apply_config= sets it,
=status()= returns it, the bar paints it, and a test proves all three.

=from_org_source= searched =doc.preamble()=, the nodes *before the
first headline*. My =config.org= put the block under =* Settings=, so
it was never found, =from_org_source= returned =Missing=, and
=load_reporting= reads =Missing= as "this vault has no config" — which
is silence by design and correct for a vault that has none.

So the whole file was ignored. Not just the error: every setting
reverted to its default and nothing said anything. The one arrangement
that worked was a bare block at the top with nothing above it, and that
is not what anyone writes — =config.org= is an org file in a vault of
org files, and giving it a heading is what everything else there does.

Two things found this and both are rules already written down.

The seam: no test could see it, because every window test builds
=GpuiView::new= directly and the whole config path ran only in
production. =run='s wiring is =apply_config= now, which =run= and the
tests both call — one path, not a copy that agrees by coincidence.

The shape: my first version of the failing test put the block at the
top of the file and *passed*. Testing the shape a person writes rather
than the shape that was convenient is what turned a missing toast into
a config file nobody could use.

Verified on :1 — the window says the file, the key, the line, what was
expected, what was got, and that defaults were used instead.


* DONE UI: an invalid view should not compile, or say why it cannot
The vision asks to apply Yesod's rule — runtime bugs into compile-time
errors — to everything, and names Slint. The gpui shell builds its
element tree imperatively: nothing stops a pane from being built with a
selector already used, a row painted outside its list, or a surface
with no key to reach it.

This is the vision item with the widest gap and the least obvious
answer, so it is allowed to end in prose. Either make a class of
invalid view unrepresentable, or write into =docs/spec.md= what gpui's
element model makes impossible, and what closure does instead — the
=debug_selector= convention and the paint tests are an answer, and if
they are *the* answer that should be stated rather than assumed.

Acceptance: =grep -c "^### Type-level views" docs/spec.md= prints 1.

Took the second option, and the argument is in =docs/spec.md= rather
than here so it is where somebody reaching for Slint will find it.

gpui builds its element tree at runtime: an outline with two rows and
one with two thousand have the same type, and so does one whose
selector is already used. Making an invalid view unrepresentable means
a different element model, which means a different toolkit — and gpui
is what makes the window fast. The vision asked for both.

What closure has instead is the selector convention, and this item did
find the hole in it: a test naming a selector nothing paints is
vacuously green, the same shape as a parser with no caller.

I tried to close that from the obvious direction — flag a test naming
an unpainted selector — and it misfired on a third of the suite,
because =assert!(debug_bounds(sel).is_some(), "…never painted")= puts
the message where a scanner looks for the selector. A guard that cries
wolf gets deleted, so it was not worth having in that shape. The sound
direction needs no guessing: a selector the window *paints* and no test
names is an affordance nobody checks. There are seventeen, ratcheted
downward, listed in the failure message.

The real answer to the vision's question is that closure buys its
type-level safety in the kernel — =BlockId= not =String=, =Support= not
a boolean, =ValueKind= not a parse per key, =Declined { reason }= not a
comment. A view that is invalid *about the vault* is already
unrepresentable before the window sees it. What is left is layout, and
layout is what a screenshot is for.

* DONE UI: the seventeen affordances no test looks at
=selectors_are_real.rs= counts them and holds the number from rising:
=bridge-row-{}=, =bridges-pane=, =detail-meta=, =flow-debug=,
=flow-detail=, =flow-graph=, =header-vault=, =image-overlay=,
=job-row-{}=, =jobs-empty=, =jobs-pane=, =palette-overlay=,
=peer-forget-{}=, =peer-{}=, =prompt-completions=, =setting-{}=,
=wk-chord-{}=.

Each is something the window paints that no test has ever asserted on,
which means it can break and stay green. Several are click targets —
=peer-forget-{}= deletes a pairing.

Lower the ceiling with each one covered. Split per pane if it helps.

17 → 15: =peer-forget-{}= and =peer-{}= are covered. Started with the
one that deletes something — a paired machine lives in =config.org=,
and forgetting the wrong row unpairs a laptop somebody still uses with
nothing failing. The test forgets the *middle* of three, because an
off-by-one that takes a neighbour is the bug it exists for and either
end would hide it.

Two things that were in the way and are now not, for the next fifteen:
=GpuiView::app= was private with no accessor, so arranging a case meant
driving the whole UI by keystroke and nobody did — =app()=, =app_mut()=
and =shell()= exist now, read-only by default because a test that wants
the window to *change* should run the command or click the target.
And the peers surface is reached by the command =sync=, not =peers=;
=peers= is the rail id. That cost a red test to discover.

15 → 10: =jobs-pane=, =jobs-empty=, =job-row-{}=, =bridges-pane= and
=bridge-row-{}=. The empty state is asserted *and* so is its absence
when there are jobs — painting both would satisfy either test alone.
That pairing is not hypothetical here: =job_rows= once read each whole
document as a cron listing, the parse failed on the first =* Headline=,
and =.ok()= dropped every job, so the pane was empty in any vault with
a headline in it. Nothing caught it because nothing asserted that a
vault *with* a job paints a row.

10 → 8: =setting-{}= and =wk-chord-{}=. Both are click targets for
somebody who does not know a chord yet, which is the reader closure
says it is for. The which-key popup's whole job is to be what you look
at when you have forgotten the key, so a popup that paints nothing
fails silently — not knowing the chord is the state you were already
in. Its selector carries the *command name* rather than the chord
letter (=wk-chord-agenda=, not =wk-chord-a=), which cost a red test.

8 → 5: =flow-detail=, =flow-graph= and =flow-debug=. All three are
=Option=s that return =None= with nothing to show, so each has two
states and asserting only the empty one is how a pane that never paints
looks healthy — both are asserted. Flows come from a real
=network.org= in the log format the sniffer's own writer produces, so
the test exercises the file rather than a constructor no user reaches.
The window has to be told to re-read the vault first: it read it on
open, and a real session gets there by the file watcher, which a test
does not run.

5 → 0. =detail-meta=, =header-vault=, =palette-overlay=,
=prompt-completions=, =image-overlay=. Four of the five are what a
reader sees most often — the palette, the metadata strip, the vault
name in the title bar — which is a fair measure of what "obviously it
works" is worth as a reason not to test something.

=prompt-completions= took three red runs and each one was my mistake,
not the code's. It needs a prompt open, a typed prefix, *and* =C-n= —
the cycle is the body editor's gesture over a prompt, not something
that fires as you type — and the candidates are TODO keywords and words
already in the vault, so my =#+= guess had none. =TO= does.

The ceiling is gone: the assertion is now that every affordance the
window paints has a test naming it, and anything new arrives with one.
That was the property the ratchet existed to approach, and it is
cheaper to hold than to regain.

Acceptance: =UNLOOKED_CEILING= in
=crates/closure-shell-gpui/tests/selectors_are_real.rs= reaches 0.


* DONE tree-sitter ships off, so the default build does not have it
=tree-sitter= is opt-in because twenty C grammars are not a hermetic
build (I10), and the default is =KeywordHighlighter=. The reasoning in
the Cargo.toml is sound and the two goals genuinely conflict.

What is not settled is whether anyone ever gets it. CI does not build
it, so the feature is not exercised by any gate; a user on the default
build has keyword highlighting and no way to know a better one exists.

Resolve it as a decision with a test behind it: vendor the grammars so
the hermetic build can have them, or keep it opt-in and add the gate
that proves both paths highlight — and say in the window which one is
running, the way the software-rasteriser notice does.

Acceptance: =nix develop -c just tree-sitter= is part of =just gates=,
or =docs/spec.md= carries the decision and =nix develop -c cargo test
-p closure-shell-gpui --features gpui-test --test highlighter_is_named=
passes.

The tension was real and the answer stays opt-in — twenty C grammars
are not a hermetic build, and I10 wins. But the item asked the wrong
question, because the feature was not merely off: it did not work when
turned on.

=highlight_spans= in closure-shell-core named =KeywordHighlighter=
directly. =pick_highlighter= exists for exactly this and its own doc
comment describes the defect — "the gpui shell, the one the user looks
at all day, reached straight for =KeywordHighlighter=, so twenty
grammars would have been compiled and never consulted" — and the call
site it was written for still reached past it. Building the window with
=--features tree-sitter= compiled the grammars and changed nothing on
screen.

Also the feature stopped at the shell crate: closure-shell-core had no
=[features]= at all, so the flag could not reach the kernel that does
the highlighting. Forwarded now.

=highlighter_name()= says which one answered, the same courtesy the
software-rasteriser notice pays: a reader whose code blocks look
plainer than expected can find out which build they have rather than
concluding highlighting is broken.

=just tree-sitter= now runs the kernel's dispatch test too, so the
grammars-on path is exercised rather than assumed. The same suite
passes both ways — a test that only ran under the opt-in would be a
test nobody runs. Verified on :1 that the default build still colours a
Rust block.

* DONE Sniffer: Block is a verdict nothing enforces
=Action::Block= exists, rules match, and =PcapBackend::capture_once=
opens a real raw socket and returns the matched action. Nothing acts on
it. The sniffer is an observer with opinions: it can tell you a
connection should not have happened, after it has.

Little Snitch — named in the vision — asks first. That is the feature.

Also =capture_once= reads exactly one frame, which is a test seam
rather than a sniffer.

(Landed as =closure-shell-core --test sniffer_enforces= and
=closure-shell-gpui --test llm_blocklist=: the enforcement point is
where the request is made, not in the crate that owns the rules.)

Scoped to what closure can honestly enforce: its own outbound traffic.
It is not a kernel module and not a proxy, so policing the machine is a
different program. The assistant is the one request closure originates
by itself, so it is the one it can refuse — before the request, which
is the whole difference between enforcing a blocklist and logging that
it was ignored.

Two things turned up on the way.

=sniffer().rules()= is only the rules toggled this session; the vault's
=sniffer_blocklist= was being rebuilt inline in the one function that
needed it. So the first working version passed a host the config
blocks. =effective_rules= is session-then-config, in that order because
=match_first= is first-match and a rule you just toggled should beat
the file.

The blocklist is asked *before* the config is judged ready. With the
checks the other way round, a blocked host reported "$KEY is not set" —
which sends the reader to fix the wrong thing and implies the key is
what stopped it. A blocked host is refused whether or not a key exists.

=ask_llm= was private and unreachable from a test, which is why nothing
enforced anything there. Public now, with =chat_last()= to ask what it
said — the send path is the one surface where "what did it do" is not
visible in a selector.

Verified on :1: the assistant answers =blocked: the sniffer blocklist
refuses http://api.example.invalid/v1= while the config banner still
says "not configured", which is the ordering above, visible.

Machine-wide interception stays out. =capture_once= reading one frame
is a test seam rather than a sniffer, and that is worth its own item if
it is ever wanted.

* DONE Conformance: record why the last two constructs stay Preserved
40 of 42 org constructs are understood and offered. The two left are
=#+ATTR_*= (export-only attributes, applied to nothing closure renders)
and diary sexp timestamps (=<%%(diary-float t 4 2)>=, which needs an
elisp evaluator).

Both should stay Preserved. But =missing:= currently reads like a
to-do, so the matrix reports them as gaps rather than as decisions, and
=the_matrix_is_worth_having= cannot tell "not built" from "deliberately
not built".

Give =Construct= a way to say "decided", with the reason, and make the
rate count decided constructs as settled. 95% that means "40 built, 2
declined, with reasons" is a truer number than 95% that means "2 to go".

Acceptance: =nix develop -c cargo test -p closure-org --test conformance_ratchet=
with a test that every non-Understood construct carries a reason.

=Support::Declined { reason }= is the third state, and the reason must
say what closure would have to *become* to do it — not that it is hard.
Both reasons are arguments somebody can disagree with: adopting org's
export layer would be a second renderer with its own idea of what a
document looks like; an elisp evaluator would privilege one language
inside timestamp syntax, in a notebook whose whole position is that the
user picks the language.

Declined constructs leave the denominator, so a decision neither
flatters the rate nor drags it down forever.

Then =the_matrix_is_worth_having= fired, exactly as expected: with the
last two declined, the matrix claimed closure does all of org it
intends to. That claim was false, and the fix was to look harder rather
than to relax the guard. Five more constructs org has and closure does
not: =#+PROPERTY:= file-wide defaults, =#+LINK:= abbreviations,
description lists, =[@3]= counter overrides, timestamp delays (=-2d=).
The guard's message now also says that declining must not become a way
to empty the list.

Being honest about those five moved the rate 95% → 88%, which exposed
the real problem: *a ratio is the wrong ratchet*. It falls when you
admit to more of org, which punishes the one behaviour this matrix
exists to encourage. So =UNDERSTOOD_FLOOR= is an absolute count (40)
that never falls for any reason, and the ratio floor may be lowered
only when the list grows. A count cannot be gamed by shortening the
list, and that is the property the percentage never had.

The CLI prints =declined= on its own line rather than as =preserved=,
because a reader deciding whether their file will work needs to tell a
decision from a backlog.

* DONE Bug: the gpui window stops presenting on the lavapipe rasteriser
Diagnosed above and not fixed: the wedge is in the presentation path
(blade/Vulkan on lavapipe), below anything this queue owns. Input is
processed, frames stop.

Reproduction: any vault, any window size, =xdotool keydown j; sleep 3;
keyup j=. First key lands, then nothing, forever.

Fixing it means either a machine with a real Vulkan driver to confirm
it is lavapipe-only, or reading blade's swapchain handling. Both are
their own piece of work and neither is a kernel or shell change.

Eliminated 2026-08-12: it is not a missing compositor. =_NET_WM_CM_S0=
is unset on :1, which looked like a promising cause — an uncomposited
X server can stall a swapchain present — but picom refuses to start
with "another composite manager is already running", so one is there
under a different selection and the wedge happens anyway.

Acceptance: reproduced on a machine with a GPU (or shown not to happen there), then fixed or reported upstream.

Closed by proof rather than by a GPU. =crates/closure-shell-gpui/examples/wedge_repro.rs=
is ~85 lines with no closure in it: one window, one counter, one key
handler that bumps it and calls =cx.notify()=, using the same
=track_focus= / =on_key_down= shape the real root element uses. It
wedges identically. So the fault is below anything this queue owns, and
that file is the artifact to file upstream.

Measured on :1, lavapipe (=VK_ICD_FILENAMES= → =lvp_icd.x86_64.json=),
gpui 0.2.2, X11:

| input                                | events the app saw |
|--------------------------------------+--------------------|
| 40 discrete press/release at 20ms    |              40/40 |
| 12 discrete at 50ms / 100ms / 200ms  |              12/12 |
| 8 discrete at 400ms                  |                8/8 |
| one key *held* 3s (X auto-repeat)    |                  1 |

Which corrects the diagnosis this item carried. It is not a rate
problem — forty keys in 800ms are all delivered and all painted — and
it is not "input is processed, presentation is dead": during
auto-repeat the app sees *one* event. The wedge is specific to X11
auto-repeat, where KeyPress repeats arrive with no intervening
KeyRelease, and gpui's X11 backend stops delivering after the first.

Practical consequence, and it is the reason this is not a release
blocker: typing works. closure took 30 discrete =j= at 20ms and moved
the cursor and repainted every time. What does not work is *holding* a
key to scroll. The startup notice already warns about the rasteriser;
it now says which gesture is affected.

The upstream report is written and not filed — filing a public issue on
somebody else's tracker is the reporter's call, not mine. Text is in
=docs/spec.md= under "A gpui bug we do not own", with the repro command
and the table above.

* DONE Notion mode: slash command menu in gpui
Acceptance: =nix develop -c cargo test -p closure-shell-core --test slash_blocks=

Already built, in both crates — =slash_menu= in shell-core and a
floating menu in gpui — with eight tests covering opening on =/= at
the start of a line, the templates it offers, accepting one, the
cursor landing inside it, backspacing past the slash, and Escape. The
item existed because the gap analysis that seeded this queue did not
check.

* DONE Notion mode: the block plus-sign affordance
Acceptance: =nix develop -c cargo test -p closure-shell-gpui --features gpui-test --test block_plus=

A =+= at the left of every outline row, in Notion mode only — a mouse
affordance on every row of a modal mode is noise for somebody who came
for the keyboard. It runs =add-sibling= through the registry (I8):
the mouse is a way of *naming* a command, not a second way of editing,
and a second path would mean a second set of rules about ids, undo and
where the new row lands.

It differs from Notion on purpose. Notion's plus makes the block
immediately; closure's asks for a title first, because that is what
=add-sibling= does everywhere else and the mouse should not invent a
different answer.

Verified on :1 — and that verification found a mistake that has been
running through this whole session: =xdotool mousemove= takes *screen*
coordinates and I had been passing window-relative ones read off
screenshots. Every earlier "the click did nothing" was me missing the
window. With the offset applied the plus selects its row and opens the
"new sibling" prompt.

* DONE Re-check the key-burst repaint bug with correct click coordinates
The evidence for it was gathered with window-relative coordinates
passed to =xdotool=, which takes screen coordinates — so "the window
takes no input and does not repaint on resize" may have been me
clicking on the desktop. The resize test used =xdotool windowsize=,
which takes a window id and is unaffected, so part of the evidence
stands and part does not.

Re-run it properly before believing any of it.

Acceptance: reproduced or closed.

Reproduced, and it is worse and simpler than the original note said.
Keys need no coordinates, so that half of the evidence always stood;
the coordinate mistake only invalidated the mouse checks.

* DONE Notion mode: mouse block drag and reorder
Acceptance: =nix develop -c cargo test -p closure-shell-core --test affordances=

Already built: =DragReorder= is the gesture machine and the drop maps
to registry moves (I8), covered in =affordances.rs= and =modal.rs=.
Verified rather than assumed.

* DONE Notion mode: parity with Doom
Every command reachable in Doom is reachable in Notion mode, by the
same registry, with its affordance shown.

Acceptance: =nix develop -c cargo test -p closure-input --test mode_parity=

Parity cannot honestly mean "the same chords" — the modes differ on
purpose, and that is what choosing one is for. Three rules that do
mean something: every mode reaches a spelled-out floor (palette,
capture, save, undo, search, manual, describe-key, describe-command);
a command bound in every *other* mode is bound here too, which is the
rule that catches a command added to four keymaps and forgotten in the
fifth; and Notion is not a stub, since it is meant to be the default.

Held already, so both rules were checked by breaking them: removing
=describe-command= from the Notion map alone gives "Notion is the only
mode that cannot reach: [describe-command]".

The fourth rule — no mode binds a name nothing implements — is not in
this file and could not be: closure-input sits below the crate that
owns the registry. =no_dead_chords= enforces it from the other side.

* DONE Self-documenting: describe-key
Acceptance: =nix develop -c cargo test -p closure-shell-core --test introspection=

Already built and bound — =g ?= in the modal keymaps, =C-h k= in
Emacs, =SPC h k= in Doom — with a surface of its own and tests in
=introspection.rs=. The item existed because the gap analysis that
seeded this queue did not check.

* DONE Self-documenting: describe-command
Acceptance: =nix develop -c cargo test -p closure-shell-core --test where_is=

=describe_command= existed with tests and nothing could reach it — no
chord, no palette entry — so where-is was a function the program had
and did not offer. It is a picker now, because every other list of
commands here is one and the question is the same: narrow, then pick.
The only difference from the palette is that Enter describes instead
of running.

Bound =g /= in the modal maps (=g ?= asks about a key, =g /= about a
name), =C-h w= in Emacs, =SPC h w= in Doom. =g w= was the obvious
choice and is =allow-flow=; the keymap test caught it.

On :1: typing "wrap" narrows to one row and Enter reports
=g W · M-z · SPC t w=, every chord that reaches it rather than the
first one the keymap lists.

* DONE Self-documenting: jump to a command's source
Acceptance: =nix develop -c cargo test -p closure-shell-core --test command_source=

The file, not the line, and the doc comment says why rather than
leaving it to be discovered: the commands are a =const= table of names
matched in a dispatch, so there is no construction site to record and
no runtime location to capture. Producing a line would mean a build
script that greps its own source — a fragile answer to a question a
grep already answers well. In a workspace of thirty-four crates the
file is the expensive half of "where does this live".

Held by three tests: every command in the palette names a file, every
file it names exists, and the file it names actually mentions the
command. A pointer into a file nobody has is worse than no pointer,
because it reads as an answer.

=describe-command= now ends with it.

* DONE Self-documenting: live docstrings in the palette
Acceptance: =nix develop -c cargo test -p closure-shell-core --test palette_descriptions=

Already built: every palette row carries its command's description,
and =palette_descriptions.rs= exists because somebody had already
noticed that 72 of 89 commands were falling back to "Run <name>" and
fixed it. Verified rather than assumed.

* DONE Bug: the manual pane does not scroll
Found 2026-08-11 while confirming I12 in the window. =M-x manual=
opens, the pane paints a scrollbar, and =C-d= / =C-u= move nothing —
so everything below the first screen of the manual is unreachable.
The manual is the shell's answer to "self-documented like Emacs", and
most of it cannot be read.

Acceptance: =nix develop -c cargo test -p closure-shell-gpui --features gpui-test --test manual_scrolls=

Three faults, not one. The cursor walked all along; nothing looked at
it (every read-only pane painted all of its rows from row zero), the
chords that move half a screen only ever moved the outline's
selection, and the pane measured its own height with the scroll
handle — which reports the *content*, so a pane that painted more
rows measured itself taller and painted more still. Fixed by
=ModalApp::pane_window= (the outline's rule, reused), by routing the
half-page chords through the keymap when a read-only pane is open,
and by measuring the viewport from the window. Verified on :1.

* DONE Bug: the manual pane double-spaces every line
Found 2026-08-11, same screenshot. Each generated line is followed by
a blank one, so the pane shows about half the text it has room for.
Either the generator emits the blank lines or the pane inserts them;
find out which before changing either.

Acceptance: =nix develop -c cargo test -p closure-shell-gpui --features gpui-test --test manual_scrolls=

Neither: no blank lines anywhere. Each row carried =py_1=, so a 12px
line painted 27.5px tall. Measuring that turned up something worse
than airiness — =pane_view= counted the viewport in 18px lines, so a
720px window was handed 37 rows, could show about 24, and =C-d= paged
straight past the other thirteen. The report said "half the text it
has room for" and the text was being skipped, not spaced.

Padding down to a pixel, and the page counted in the same number the
rows paint in. Rows wrap now, so their height is not a constant and no
arithmetic here can predict it; the count errs small on purpose, and
what the test holds is the property that matters — wherever the cursor
goes, it is somewhere you can see.

* DONE Build: target/ grows until the disk stops the work
Found 2026-08-12. =target/= reached 75G — 53G of it debug, 12G of
that incremental — and the root filesystem hit 98%, at which point
every command in the session failed before it ran and the linker's
own temporaries had nowhere to go. A gpui link failure that looked
like a real error was probably this.

Clearing =target/debug/incremental= bought 12G and costs only a slower
next build, which makes it the obvious first move; it should not have
taken a full disk to discover that.

Acceptance: =nix develop -c just reclaim=

=just reclaim= drops the incremental caches, prints free space before
and after, and names the next two steps in order of what they cost to
lose. Run here: 9.6G -> 16G, and the 6.4G it removed had accumulated
during this session alone, which is the argument for the recipe
existing rather than a comment somewhere.

* DONE Bug: C-d in a read-only pane steps by the outline's height
=half-page-down= computes =let step = (self.outline_view.height / 2)=
and then, if a read-only pane is open, moves =pane_cursor= by it. That
is half the *outline's* viewport — a count of single-line rows in a
different list. Pane rows wrap, so the pane holds fewer, and the cursor
moves further than the pane shows.

Independent of the pane-measurement item above and much smaller: the
pane needs to report its own page the way the outline reports
=set_outline_viewport= and the body reports =set_body_viewport=. Even
the current conservative =pane_view()= is the right number to report;
it is simply not the one being used.

Acceptance: =nix develop -c cargo test -p closure-shell-gpui --features gpui-test --test manual_scrolls=
passes with a new case asserting that a pane's half-page differs from
the outline's when the two lists are different lengths.

Measured before the fix: the manual painted 15 rows and one =C-d=
stepped 15. So a "half page" was a whole one and every press skipped
half the text — which is the user-visible half of the original report
("the pane shows about half the text it has room for") and had nothing
to do with the wrap guess.

The assertion that catches it has to compare the step to what was
*painted*. =step <= painted= is too weak: 15 <= 15 passes. =step * 2 <=
painted + 1= is the claim.

=set_pane_page= is the same split the outline and the body already
make: the window measures, the core decides where the cursor goes. The
pane was the one list without it.

=half_a_page_scrolls_the_pane= had to be rewritten in place, dated,
with the reason. It asserted that five presses reach =manual-row-40=,
and that threshold quietly encoded the defect beside it — five whole
pages is seventy-five rows. It asks "did the pane move, and by pages"
now, which is the claim it was always making.

The field ratchet refused =pane_page= and was right again: the pane had
a cursor of its own with no home either. =PaneView= took both. The
ceiling stays at 89 — a cluster that absorbs a new field is holding the
number, not lowering it.

Window check: the window opens and paints correctly. I could not drive
=C-d= into it by synthetic keystroke — the display stopped accepting
them, which is consistent with the auto-repeat wedge documented above
and with the held-key bursts this session had already sent it. Said
plainly rather than claimed: the behaviour is covered by seven passing
window tests that drive the real element tree, and the step-versus-
painted numbers above came from that harness.

* TODO Panes: lay rows out until the pane is full, instead of guessing
=PANE_WRAP_GUESS= assumes a pane row takes two lines, because rows
wrap and nothing here can know how many. Overshooting skips content,
so it errs small, which means =C-d= moves less than a screen when
lines are short.

A list that measures rows as it places them would need no estimate.

Acceptance: =PANE_WRAP_GUESS= is gone and =manual_scrolls= still passes.

Attempted 2026-08-12 and reverted. The idea was sound and the
measurement was not available.

Average row height — the pane's content height divided by the rows
painted last frame — is the right quantity, and specifically *not* the
content height itself, which is the trap this pane already fell into
once: painting more rows made it measure taller and ask for more rows.
An average cannot run away, because painting more rows does not make a
row taller.

What is missing is a trustworthy content height. =side_scroll.bounds()=
is the only handle on offer and it has now behaved two different ways
in two investigations — content-shaped once, viewport-shaped once — so
the derived page came out identical at 1600px and 700px wide, and the
cursor-visibility guarantee broke. Shipping that would have traded a
documented, conservative estimate for an undocumented wrong one.

The test that would prove it, for whoever picks this up: paint the
manual at 1600x720 and at 700x720, count the painted =manual-row-*=
selectors, and require the narrow pane to fit fewer. It fails today at
16 and 16.

What it probably needs is gpui's own measured list rather than a
height computed ahead of layout.

Second attempt 2026-08-13, also reverted. Further than the first, and
the measurements below are the useful part — the next attempt should
not have to re-derive them.

*The proving test now exists and is sound.* Two windows, one per width,
because =debug_bounds= keeps every selector it has ever seen: resizing
one window and counting twice gives a running total, which is how my
first run had the narrow pane fitting *more* rows than the wide one (24
against 15, where the 24 was 15 wide rows plus 9 narrow). A window each
gives 15 and 15 — width-independent, as the item said.

*Row geometry, measured on a real frame*: an unwrapped row is 21.5px, a
two-line row 41px, a five-line row 99px. So the first line costs 21.5
and each one after it costs 19.5 — =n * PANE_ROW_H= overshoots by the
padding a row pays once, and the error accumulates down the pane.

*Pane chrome is 78.5px, not =BODY_CHROME='s 46.* The manual's first row
is painted at y=78.5; a pane carries the hint line the editor does not.

*=body_cols()= is the wrong width source here*, and this is what the
first attempt tripped on: it reads =side_scroll.bounds()=, which is
content-shaped. The window's own =viewport.0= minus the rail and the
outline is not, and does vary with the window.

*The blocker is =pane_window='s two reachable offsets.* It places the
window ending at the cursor — except at the bottom of the list, where
it clamps to the last =page= rows and the cursor sits somewhere inside
them. A page that fits the first shape overflows the second (cursor at
803px in a 720px window); taking the =min= of both fits everything and
is so conservative that =half_a_page_scrolls_the_pane= stops moving.
Sizing a window that has two shapes needs =pane_window= to have one —
that is the next thing to change, not the arithmetic.

*A real bug found on the way, and worth its own item*: =C-d= in a
read-only pane steps by =outline_view.height / 2= — half the
*outline's* height, a measurement of a different list whose rows do not
wrap. That is independent of the guess and is why paging in the manual
skips more than it looks like it should.

* DONE Bug: the read-only panes clip long lines instead of wrapping
Found 2026-08-11 in the same screenshot as the scroll fix. Manual
rows run off the right edge mid-word — "Add a sibling headl" — so a
command's keys, which are at the end of its line, are exactly what
gets cut.

Acceptance: =nix develop -c cargo test -p closure-shell-gpui --features gpui-test --test manual_scrolls= covers wrapping.

The same flex shape as the detail pane's own long-title bug: a flex
item will not shrink below its content, so the text pushed the row
wider than the pane. Basis zero on the text, and a width on the row
for it to resolve against — =flex_1= against nothing is nothing.

Bounds cannot see this: the row stays inside the pane and the text
overflows it. What a wrapped row does is get taller, so the test
compares row heights and was checked both ways — without the fix every
row is 27.5px.

* DONE Perf: landing on a headline still walks its subtree once
Found 2026-08-11 while writing the I11 keystroke budget. The five
passes are gone, but =children_source_preview= calls =subtree_end=,
which walks to the last descendant to find where the subtree ends —
so landing on a headline with 5,000 sections costs 211 us against 9.5
us for a leaf. Inside the budget, and still O(subtree) per keystroke,
which is the shape the traversal work set out to remove.

The end of a subtree is a prefix-sum question like the counts were.

Acceptance: =nix develop -c cargo test -p closure-shell-gpui --test budget_keystroke=.

The diagnosis above is wrong and the measurement disproves it.
=subtree_end= recurses only into =children.last()=, so it is O(depth),
not O(subtree). Landing on the chapter costs 110, 95 and 96 us for
subtrees of 1,000, 5,000 and 20,000 sections — twenty times the
subtree, the same cost. The ten-fold gap against a leaf is the preview
materialising up to PREVIEW_CAP lines where a leaf has two, which is
the cap doing its job.

Closed by a test that holds what is actually true — the cost of
landing does not grow with the subtree — rather than by a change that
was not needed. The 211 us in the earlier note was a single cold
measurement; repeated, it is 95.

* DONE Coverage: raise the floor to the current measured value
The floor is 84. Measure, raise it to what the workspace actually
holds, and record the number here. A new item goes in whenever it
drifts up again.

Acceptance: =nix develop -c just coverage= passes at the raised floor.

84 -> 86. Lines 86.08%, regions 83.37, functions 83.48. The justfile
comment said there was "no integer headroom left to claim" at 84.33,
which was true of the code as it stood and wrong as a prediction: this
run added a few thousand lines of product code and every one of them
arrived behind a failing test first, so the hermetic ceiling moved
about two points rather than a fraction. The residual ~14% is the same
non-hermetic set as before — the ratatui draw loop, the curl/HTTP
paths, the GUI window and socket loops.
