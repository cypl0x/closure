[package]
name = "closure-lsp"
description = "LSP bridge: translates LSP requests to registry commands (I8)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
closure-core = { path = "../closure-core" }
closure-store = { path = "../closure-store" }
closure-config = { path = "../closure-config" }
closure-jsonrpc = { path = "../closure-jsonrpc" }
thiserror.workspace = true

[dev-dependencies]
tempfile = "3"
