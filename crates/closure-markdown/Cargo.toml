[package]
name = "closure-markdown"
description = "CommonMark + GFM byte-exact roundtrip parser (Layer 1)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
thiserror.workspace = true

[dev-dependencies]
# Property/fuzz tests: I1 byte-exact roundtrip + I5 no-panic + I6
# determinism on random markdown-ish input (D1). Gate: `just fuzz`.
proptest.workspace = true
