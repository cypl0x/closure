//! ratatui + crossterm shell for the closure kernel. Renders the tree, drives
//! the command palette, the which-key popup, fuzzy finder, and the undo-tree
//! visualiser. Implementation lands in M6.

#![forbid(unsafe_code)]
