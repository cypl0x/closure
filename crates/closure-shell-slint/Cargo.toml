[package]
name = "closure-shell-slint"
description = "Slint declarative-UI shell descriptor over the closure vault (Layer 5, Q13)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
closure-core = { path = "../closure-core" }
closure-store = { path = "../closure-store" }
closure-shell-core = { path = "../closure-shell-core" }

[dev-dependencies]
tempfile = "3"
