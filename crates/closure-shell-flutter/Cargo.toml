[package]
name = "closure-shell-flutter"
description = "Flutter embedder for closure (cross platform mobile/desktop). Suggestion per vision; matrix entry."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
# flutter engine bindings or FFI when bodied; stub for now.
