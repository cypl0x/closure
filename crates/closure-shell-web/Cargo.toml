[package]
name = "closure-shell-web"
description = "Self-contained HTML + localhost web shell (Layer 5). Consumes closure-core only (I7)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
closure-core = { path = "../closure-core" }
closure-query = { path = "../closure-query" }
closure-store = { path = "../closure-store" }
closure-shell-core = { path = "../closure-shell-core" }
thiserror.workspace = true

[dev-dependencies]
tempfile = "3"
