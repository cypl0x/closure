/target
/result
/result-*
/.direnv
/.env
/.envrc.local

# per-crate
**/target

# IDE
/.idea
/.vscode
*.swp
*.swo

# coverage / bench artifacts
/tarpaulin-report.html
/coverage
/criterion

# fuzz
/fuzz/target
/fuzz/corpus
/fuzz/artifacts

# grok CLI session state
.grok/

# flatpak-builder's cache: a copy of the source tree plus every crate
# it downloaded. Ignored so it is neither committed nor walked —
# treefmt reformatted 9,310 vendored files inside it once, which took
# five minutes and failed the gate.
/.flatpak-builder/

# Flutter build output and pub state (the Dart side is outside the
# hermetic gate; none of this is reproducible and none of it belongs
# in the repo).
flutter/build/
flutter/.dart_tool/
flutter/*.iml
